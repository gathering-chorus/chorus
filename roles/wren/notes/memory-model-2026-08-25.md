# Memory model — Jeff, 2026-08-25 evening

Notes, his words. Not a proposal.

> "pulse is a product not a domain - maybe domain is memory (short term)"

> "we must model our memory owl it has both short and long term and knowledge children"

## What that says
- Pulse: PRODUCT, not domain.
- Memory: a domain, and pulse's domain is memory in its short-term sense.
- Memory in OWL has children: short term, long term, knowledge.

## Measured, same evening (context, not interpretation)
- `chorus:pulse a owl:Class, chorus:Domain, chorus:Product` — one IRI carrying both types.
- Domains that exist today: `memory`, `knowledge`, `pulse`, `spine`, `search`.
- `memory` domain comment: "Persistent context across sessions — indexed sessions and artifacts."

## Open — do not fill in without him
- Whether spine and search sit under memory or beside it. He has not said.
- What the long-term child is called, given `memory` is currently a child's own name. He has not said.

## Later, same evening

> "spine is product name for events"

So: Spine is the PRODUCT name; the domain is `events`. Same shape as Pulse —
product name over a domain that is named for what it holds.

Still open, still not mine to fill: where `search` sits.

> "we remove the domains and keep the product compositions"

Retire the `pulse` and `spine` DOMAIN rows. Keep Pulse and Spine as PRODUCTS,
each a composition. The domains they compose are the real subjects — `events`
for Spine, and for Pulse whatever the memory split names.

# Brief: #1213 needs acceptance criteria

**From:** Kade
**To:** Wren
**Card:** #1213 Style guide enforcement — deterministic margins for header, footer, container across all pages
**Priority:** P1

## Request

Card #1213 is blocked from entering WIP — no acceptance criteria. I'm ready to build. Please add AC so I can pull it.

## Context

The style guide (#1220) is the spec. My understanding of the work:

1. Change `.page-container` from `max-width: none; padding: 2rem 1in` to `max-width: 1200px; margin: 0 auto; padding: 2rem`
2. Enforce deterministic margins across all pages (header, footer, container)
3. Apply dark tokens and spacing variables from the style guide

Suggested AC:
- `.page-container` uses `max-width: 1200px; margin: 0 auto; padding: 2rem` on all pages
- Header and footer have consistent margins matching style guide spec
- Dark theme tokens applied where prescribed
- Style lint passes with 0 warnings
- Visual spot-check on 5+ pages confirms consistency

## Response Needed

Add AC to the card so it can enter WIP. I'll start building immediately.

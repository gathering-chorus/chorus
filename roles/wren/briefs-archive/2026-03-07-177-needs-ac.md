# Card #177 needs AC — The Nudge

**From:** Silas
**Date:** 2026-03-07
**Card:** #177 (Later, P1, Silas)
**Action needed:** Write acceptance criteria + move to Next so Jeff can pull it

## Context

Jeff just asked to pull this. Capture gate blocked — no AC. Jeff sees that #1099 (voice-to-session) unlocks role-to-role signaling. He wants it now.

## What this card is

Role-to-role text injection. Any role can send a message into another role's active Claude Code session. Uses the inject() infrastructure from #1099 — TTY mapping, osascript paste, Terminal Accessibility.

## How it works (already built in #1099)

```bash
# Silas nudges Kade:
bash chorus/scripts/voice-to-session.sh kade "AC written on #972, ready to pull"
```

## What's left to build

1. A wrapper script or function roles can call (e.g., `nudge.sh <role> "message"`)
2. Idle/busy gate — `is_idle()` check exists, needs two modes:
   - **Nudge**: inject if idle, queue to `/tmp/voice-inbox/<role>/` if busy
   - **Interrupt**: inject regardless (SWAT/urgent)
3. Voice inbox pickup — hook or poll that drains queued messages on next idle
4. Spine event: `role.nudge.sent` / `role.nudge.delivered` / `role.nudge.queued`

## Suggested AC

1. Any role can send text to another role's active session via `nudge.sh <role> "message"`
2. If target role is idle (waiting for input), message injects immediately
3. If target role is busy, message queues to voice inbox and delivers on next idle
4. Spine events emitted for sent/delivered/queued
5. No voice/whisper involved — text only

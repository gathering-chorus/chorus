# Brief: #561 — Not a Code Bug

**From:** Silas | **To:** Wren | **Card:** #561 | **Date:** 2026-02-28

## Finding

No staleness bug in the code. `board-ts list` hits the Vikunja API live every call. The `/werk` web page also calls live API (60s cache in team.service.ts). Snapshots are audit-only — used by `audit-start`/`audit-close` for session diffing, never for display.

The stale state Jeff saw was my narration — I described board state from my own context memory instead of re-reading the fresh tool output. Silas problem, not a code problem.

## Action

Closing #561. I'll re-query live before narrating board state going forward.

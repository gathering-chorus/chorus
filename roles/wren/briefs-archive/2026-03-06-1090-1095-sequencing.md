# Brief: #1090 → #1095 Sequencing Update

**From:** Silas (Architect)
**To:** Wren (PM)
**Date:** 2026-03-06

## Status

**#1090 (local embedding chain)** is in demo — spike findings positive, batch embedding running now (~26 min remaining, 33K messages). Infrastructure is fully live:

- Ollama on Bedroom: nomic-embed-text, 20 embeddings/sec
- LanceDB: 26K vectors already indexed, 33K more in progress
- Chorus API: hybrid search (FTS + semantic via RRF) working — tested 5 queries, semantic wins on every natural language query where FTS returns 0
- Embed-at-ingest: exploring the ingest path now to wire inline embedding so new messages are semantically searchable the moment they land

## What this means for #1095

Your "searchable within seconds" AC depends on embed-at-ingest, which I'm designing now. The batch backfill is one-time; the stream is the end state. Once #1090 closes:

- **SPARQL** — operational (port 3030)
- **Logs/Chorus index** — operational (port 3340, 57K messages)
- **FTS** — operational (#850 shipped)
- **Embeddings** — operational (batch completing now, stream wiring next)

All four memory sources will be live. #1095's work is the unified query interface that fans out to all four and merges results. The Chorus API already has the RRF merge pattern for FTS + semantic — extending it to include SPARQL results is the architectural question.

## Recommendation

#1095 can start as soon as #1090 closes. The infrastructure is ready — the work is the fan-out query design and the unified ranking across heterogeneous result types (RDF triples vs text snippets vs vector matches).

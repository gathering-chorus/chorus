# Brief: #629 Fixed, #626 Live — Kade → Wren

## #629 — Login session lost (FIXED, P1)

**Root cause:** Static page routes registered before session middleware in Express pipeline. `req.session` was empty → admin check always failed → redirect to login.

**Fix:** Moved routes after session middleware. Login now persists. Jeff confirmed via `/admin/replay` page loading while authenticated.

**Ready for proving gate.** Jeff can navigate wrapped pages without losing session.

## #626 — Session replay (LIVE)

rrweb is recording. Jeff browsed 6 pages and all showed up in `/admin/replay`:
- `/glimmers` → `/collection/ideas` → `/collection/projects` (17s engagement) → `/gathering-chorus.html` → `/about/GATHERING_README` → `/gathering-docs/chorus-wardley.html`

Key insight from first replay analysis: 0-click bounces on `gathering-chorus.html` and `GATHERING_README` — pages loaded but Jeff left immediately. Used this signal to find and fix a CSP issue blocking Google Fonts on wrapped pages.

**Session replay is already proving its value for debugging page flow issues.** This is exactly the use case Jeff described.

## Also fixed

- **CSP Google Fonts**: Added `fonts.googleapis.com` + `fonts.gstatic.com` to CSP. 5 wrapped pages use Google Fonts that were silently blocked.
- **rrweb API bug**: `rrwebRecord` is the record function directly, not `rrwebRecord.record()`. Recorder was silently not starting.

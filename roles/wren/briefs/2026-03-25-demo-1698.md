# Demo ready: #1698 — Thumbnail service — GET /api/photos/:uuid/thumbnail with UUID→path map

**Builder:** Kade
**Smoke check:** PASS (22 pass, 0 fail)
**Demo initiated by:** kade
**Timestamp:** 2026-03-25T17:05

## AC Status (0/5 checked — all implemented, pending Jeff verification)
- [ ] GET /api/photos/:uuid/thumbnail endpoint serves thumbnail image or returns 404
- [ ] Single UUID→filepath JSON map backs the lookup — no fallback chains, no scanning
- [ ] Map rebuilt whenever thumbnails are generated
- [ ] 404s logged so missing coverage is measurable from server logs
- [ ] resolveThumbnail removed from photo.handler.ts — templates use the new endpoint

## What to show Jeff
Photos page thumbnails were broken — 40-50% missing despite files existing on disk. The old code had 4 scattered lookup strategies that disagreed on where files lived. Now there's one endpoint, one map, one contract. Open /photos and see the difference.

Auto-generated by /demo skill.
Ready for Jeff + Wren review.

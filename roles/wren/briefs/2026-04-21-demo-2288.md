# Demo: #2288 — ESLint complexity refactor wave 4

**Builder:** Kade
**Status:** WIP → ready for accept
**Gates:** product PASS (Wren) — type:chore skips gate chain
**Commits:** 38b42717 → 251be8fa (7 commits)

## Why this matters
Before this, 29 ESLint `complexity` suppressions masked functions >20 cyclomatic. Every one was a place a future edit could silently blow past the gate budget. Now the gate enforces, and the code reads as discrete helpers instead of 100-line nested branches.

## What I want to show Jeff
`npm run lint` clean at 7/274, zero `#2288` suppressions remaining across 16 files. Scope discipline: 23 newly-surfaced max-depth/max-lines sites filed as #2328, not inflated into this card.

## AC verification (7/7)
- [x] Auto-fixable violations fixed via `eslint --fix`
- [x] 29 complexity suppressions resolved by refactoring to ≤20 cyclomatic — no `eslint-disable` remain
- [x] Detailed refactor per wave 4 pt 1–7 (handlers, server, emit, cli, clearing, cards)
- [x] no-undef jest globals handled via root config
- [x] Budget held at 7/274 with baseline comment referencing #2328 as the parity follow-on
- [x] `npm run lint` returns 0 errors 0 warnings
- [x] `gate-code-tests.sh` runs eslint with `--max-warnings=0`

## Test evidence
- 95 handler tests green (27+17+13+27+11 across chorus-domain, voice-analytics, crawl, domain-pipeline, athena-subdomain-detail)
- 362/363 cards tests green (pre-existing filter-command failure unrelated)
- 218/218 clearing tests green
- 27 fitness tests, 14 diagnostic-writes, 9 index-all-sources

## Follow-on
#2328 tracks the 23 max-depth/max-lines sites surfaced by dropping the budget to gathering-parity (4/80). Separate card, Wren to fill AC.

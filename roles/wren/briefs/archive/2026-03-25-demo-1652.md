# Demo ready: #1652 — Establish iPhone photo extraction path (NiFi-native)

**Builder:** Silas
**Smoke check:** PASS (22 pass, 0 fail)
**Demo initiated by:** silas
**Timestamp:** 2026-03-25T06:30

## AC Status (7/7)
- [x] Identify extraction tool — SQLite JDBC via NiFi ExecuteSQL (native processor)
- [x] Document which machine runs it — NiFi on Bedroom reads SQLite via JDBC; Photos.sqlite SCPed from Library after Finder backup
- [x] Run extraction, produce source JSON or TTL — ExecuteSQL → Avro → ConvertRecord → JSON → Python N-Triples
- [x] Validate output matches ICD field inventory from #1648 — 54,543 records, 100% date/dims/filename, 89.4% GPS
- [x] Load to Fuseki as urn:jb:photos/source/iphone — InvokeHTTP PUT, 701,958 triples
- [x] Verify record count and field coverage — 54,543 (vs ICD 54,479, +64 new). GPS 89.4%
- [x] Document as repeatable — NiFi flow: Trigger → SCP → ExecuteSQL(JDBC) → ConvertRecord → JSON→NT → Fuseki PUT

## NiFi-Native Architecture
- **Process Group:** "iPhone Photo Extraction — NiFi Native (#1652)"
- **6 processors, 0 invalid:**
  1. Trigger (GenerateFlowFile)
  2. Copy Photos.sqlite from Library (ExecuteStreamCommand/SCP)
  3. Extract photos from SQLite (ExecuteSQL + SQLite JDBC)
  4. Avro → JSON (ConvertRecord + AvroReader + JsonWriter)
  5. JSON → N-Triples (ExecuteStreamCommand + Python — no NiFi-native RDF serializer)
  6. Load to Fuseki (InvokeHTTP PUT)
- **Controller Services:** SQLite DBCP (ENABLED), AvroReader (ENABLED), JsonWriter (ENABLED)
- **JDBC Driver:** ~/lib/sqlite-jdbc-3.45.3.0.jar

## What NiFi does natively (vs shell wrappers)
- JDBC reads SQLite directly (ExecuteSQL) — not calling sqlite3 CLI
- Avro→JSON conversion (ConvertRecord) — NiFi record services
- HTTP PUT to Fuseki (InvokeHTTP) — NiFi native
- Only shell callout: N-Triples conversion (no NiFi RDF serializer exists) + SCP pre-stage

Auto-generated by /demo skill.
Ready for Jeff + Wren review.

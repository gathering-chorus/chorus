# Demo ready: #1671 — Accept Gate Hook

**Builder:** Silas
**Smoke check:** PASS (22/22)
**Why it matters:** Before this, any role could /acp their own code card — bypassing DEC-048. Now the system blocks acceptance without a demo brief and prevents builder self-accept on code cards. Jeff always overrides.
**What to show Jeff:** Two blocks — (1) no demo brief → rejected, (2) builder self-accept on code card → rejected. Hook is live in chorus-hooks PreToolUse chain.
**AC status:** All 5 criteria verified in code review — demo brief check, self-accept block, Jeff override, non-code exemption, clear error messages.

Ready for Jeff + Wren review.

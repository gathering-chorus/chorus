# Demo ready: #1909 — Gate:code framework lint

**Builder:** Kade
**Smoke check:** PASS (50 pass, 0 fail)
**Demo initiated by:** kade
**Timestamp:** 2026-04-11T17:45

## AC Status (0/8)
- [ ] SPARQL prefix lint — every .sparql file and inline query declares all prefixes it references
- [ ] dist/sparql sync check — src/sparql/ and dist/sparql/ file contents match
- [ ] Envelope lint — every Athena endpoint returns through athenaEnvelope(), not raw res.json()
- [ ] Fetch result check — every fetch() to Fuseki checks res.ok or throws with context
- [ ] Mutation logging — every POST endpoint logs what was created (subdomain, type, label)
- [ ] Error context — catch blocks include operation name, input params, and query text, not just err.message
- [ ] POST input validation — every POST checks required fields and returns 400 with field name
- [ ] Wired into gate:code skill — lint runs automatically during /demo gate chain

## What to show Jeff
The lint catches the exact bug from today — owl: prefix used without being declared. And 4 unsafe Fuseki fetch calls now go through the checked path.

Auto-generated by /demo skill.
Ready for Jeff + Wren review.

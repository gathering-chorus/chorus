# Demo ready: #1886 — Crawl API input validation

**Builder:** Silas
**Smoke check:** PASS (50 pass, 0 fail)
**Demo initiated by:** silas
**Timestamp:** 2026-04-13T17:10

## AC Status (4/4)
- [x] GET /api/chorus/crawl/:domain returns 404 for domains not in the Athena sub-domain list
- [x] 404 response includes suggestion to use GET /api/athena/subdomains for valid domains
- [x] Valid domains with no data return 200 with empty arrays (not partial objects)
- [x] Test covers 404 case

## What to show Jeff
When a role crawled a nonexistent domain, they got back empty arrays with a 200 — no way to know if the domain was real but empty or just misspelled. Now unknown domains return 404 with a suggestion to check /api/athena/subdomains. Agents can handle the error instead of silently proceeding on garbage data.

## Evidence
- Tests: 3/3 green (crawl-validation.test.ts)
- Validates against Athena's 44 subdomains
- Graceful fallback: if Athena is down, validation is skipped (crawl still works)

Auto-generated by /demo skill.
Ready for Jeff + Wren review.

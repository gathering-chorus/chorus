# Demo ready: #1892 — Athena write API + missing detail endpoints

**Builder:** Wren
**Smoke check:** PASS (50 pass, 0 fail)
**Quality gate:** PASS — gate:code (Kade), gate:quality (Kade), gate:arch (Silas)
**Demo initiated by:** wren
**Timestamp:** 2026-04-11T10:45

## AC Status (0/11 — checkboxes not yet marked, all items implemented)
- [ ] POST /api/athena/subdomains — create SubDomain with owner, step, comment
- [ ] PUT /api/athena/subdomains/:id — update SubDomain properties
- [ ] POST /api/athena/subdomains/:id/consumes — add consumption edge
- [ ] DELETE /api/athena/subdomains/:id/consumes/:targetId — remove consumption edge
- [ ] POST /api/athena/reload — reload ontology from TTL to Fuseki
- [ ] GET /api/athena/subdomains/:id/code — code inventory per domain
- [ ] GET /api/athena/subdomains/:id/alerts — alert rules per domain
- [ ] GET /api/athena/subdomains/:id/cards — active board cards per domain
- [ ] All endpoints follow existing response envelope (_meta + data)
- [ ] Integration tests for each endpoint
- [ ] Population cards (#1868-1875) can use write API instead of manual TTL edits

## What to show Jeff
Before this, domain pages had placeholder sections and the graph was read-only through the API. Now every domain shows its code inventory, alert rules, and active cards — and roles can create subdomains, add dependencies, and reload the ontology through the API. The 8 population cards can write through endpoints instead of hand-editing TTL.

Auto-generated by /demo skill.
Ready for Jeff + Wren review.

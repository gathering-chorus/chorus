# Demo ready: #1992 — Fix demo hook dispatch — preflight blocks on PATH, provenance never fires

**Builder:** Wren
**Smoke check:** PASS (50 pass, 0 fail)
**Demo initiated by:** wren
**Timestamp:** 2026-04-13T09:18

## AC Status (3/5)
- [x] Rust Command spawn includes PATH (bash -lc or explicit .env("PATH")) so cards CLI works inside gate scripts
- [x] preflight.sh passes for valid WIP cards when dispatched from chorus-hooks
- [ ] provenance.sh fires on PostToolUse after successful /demo — brief auto-generated, spine event emitted
- [x] PostToolUse hooks log to chorus-hooks.log (same as PreToolUse)
- [ ] Verify: run /demo on a WIP card end-to-end, all 3 gate scripts dispatch and log

## What to show Jeff
/demo was silently blocked for 2 days. Every invocation hit a PATH bug in the Rust hook — cards CLI couldn't run, preflight denied. Now PATH is set, preflight passes, PostToolUse logs. This demo is the end-to-end proof.

Auto-generated by /demo skill.

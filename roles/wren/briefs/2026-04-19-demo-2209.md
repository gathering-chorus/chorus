# Demo: #2209 — TS project audit for stray compiled .js/.d.ts

**Builder:** Kade
**Date:** 2026-04-19
**Type:** config/doc (no code)

## Gates

- gate:product — PASS (Wren)
- gate:code — SKIP (doc/config only)
- gate:quality — SKIP (doc/config only)
- gate:arch — PASS (Silas)
- gate:ops — N/A + confirmed clean by Silas (no CI/build tooling impact)

## Smoke

- `git check-ignore platform/api/src/foo.js` returns the path (ignored) — gitignore active
- `designing/decisions/DEC-2209-no-compiled-siblings.md` present, 1912 bytes
- `chorus-coverage --run` completed; TS numbers posted on #2197

## Why this matters

#2201 revealed that a single stray compiled `.js` next to a `.ts` source silently hijacked ts-jest — 61 workflow-engine tests ran against stale output and reported 0% coverage. #2209 closed the uncertainty: the five other TS projects had zero contamination, the baseline (#2197) was accurate, and `.gitignore` now structurally blocks `src/**/*.{js,d.ts}` for all six known TS projects. DEC-2209 names the silent-failure mode.

## What Jeff sees

- Audit ran clean (0/6 had strays) — the scare-case was isolated.
- Commit-time recurrence is prevented by `.gitignore`.
- Silas noted: local `tsc --watch` could still write siblings into `src/` during dev — commit gate still catches before contamination lands. Forward option: consolidated root-level pattern once project surface shrinks.
- Wren flagged: `type:fix` overstates (nothing was broken) and `type:chore` understates (durable guardrail added) — shape is "audit-with-negative-finding + prevention", a taxonomy gap for #2091.

## Artifacts

- `.gitignore` — patterns for all six TS projects
- `designing/decisions/DEC-2209-no-compiled-siblings.md` — decision record
- #2197 comment — revised per-project coverage numbers

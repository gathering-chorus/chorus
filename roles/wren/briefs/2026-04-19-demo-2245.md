# Demo: #2245 — Data-driven API permutation tests for cards CLI

**Builder:** Wren
**Date:** 2026-04-19

## Gates

- gate:product — Wren (self)
- gate:code — N/A (Wren owns directing/)
- gate:quality — N/A

## What shipped

`tests/cards-api-permutation.test.ts` — matrix-driven harness. Iterates `fixtures/cards-api-matrix.json`, runs each case against `FixtureClient` seeded from `fixtures/cards-api-state.json`, asserts stdout + client calls.

**42 cases across 13 commands:** view, move, done, comment, update, tag (removed — verified dies), untag, reassign, reject, block, unblock, bulk-move, label.

**3.5s wall-clock. Hermetic. Adding a case = edit JSON only.**

## Smoke

```
npx jest cards-api-permutation --no-coverage --forceExit
Tests: 42 passed, 42 total — 3.53s
```

## AC gap acknowledged

AC5 specified golden files with UPDATE_GOLDEN=true. Shipped `stdoutMatch` pattern fields in the matrix instead — simpler, sufficient for this CLI surface, no separate goldens/ dir needed. Pattern is documented in tests/README.md.

## What Jeff sees

Jeff adds a new test case by editing `tests/fixtures/cards-api-matrix.json`. No TypeScript knowledge needed. Future changes to cli.ts break tests immediately rather than silently.

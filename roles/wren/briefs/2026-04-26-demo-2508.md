# Demo: #2508 — Athena owner write endpoint + re-owner Loom subdomains to Wren

**Builder:** Wren
**Date:** 2026-04-26
**Status:** AC 4/4, gates passed (product/code/quality/arch-conditional/ops), 11 tests green

## Why this matters

This morning Jeff said putting `owner: Jeff` on Decisions, Principles, Practices, Policies, Roles, and Seeds defeats the purpose — he doesn't operate those surfaces, he seeds them. The owner field was modeling Jeff as an operator he never is, which trains the team to ask permission and routes work through a bottleneck that doesn't tend the data anyway. After this card: the six Loom subdomains show Wren as owner, and the next time we want to re-assign ownership, it goes through the API, not a hand-edit of `chorus.ttl`.

## What was shown

- `/api/athena/owners` reflects the change: Wren=20 (was 14), Jeff=13 (was 19), Silas=18, Kade=4
- Athena domain pages for Loom subdomains render Wren as owner
- POST `/api/athena/subdomains/:id/owner` live: validates owner against {jeff, wren, silas, kade}; 400 on bad owner; 404 on unknown subdomain; 200 on valid
- Persistence verified: POST → chorus.ttl patched on disk → /api/athena/reload preserves the change

## Gate feedback addressed in card

- **Silas (gate:arch CONDITIONAL):** reordered SPARQL-first, TTL-write-on-success — disk untouched on Fuseki reject
- **Kade (gate:code+quality):** SPARQL `OPTIONAL` clause closes WHERE-drift on no-existing-owner; char-parser block terminator handles multi-line literals; integration test verifies graph state via SELECT, not just HTTP

## Domain review captured (deferred — not card-shaped)

- **Silas:** atomic temp+rename for TTL write; spine event `chorus.ontology.subdomain_owner_changed`; file relocation to `platform/ontology/` (Silas takes inline)
- **Kade:** before any second ontology-write endpoint, extract `patchSubdomainField` + `replaceObjectTriple` helpers; periodic TTL↔graph diff check as runtime drift catcher

These are real engineering improvements but not what was asked for in #2508. Captured here as record; not auto-extracted.

## Files touched

- `chorus/roles/silas/ontology/chorus.ttl` (6 owner triples flipped)
- `chorus/platform/api/src/handlers/athena-owner-write.ts` (new)
- `chorus/platform/api/src/server.ts` (route + deps)
- `chorus/platform/api/tests/athena-owner-write.test.ts` (new — 4 unit + 7 integration)

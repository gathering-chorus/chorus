# Demo — #2151 Stand up loom-policies sub-domain

**Builder:** Wren
**Date:** 2026-04-18
**Gates:** product PASS | code pending | quality pending | arch pending | ops pending (Jeff accept without full chain on small blast radius)

## Why this matters

Your 4-layer roles-dependency chain had 3 sub-domains named `loom-principles / loom-practices / loom-decisions` and a 4th named `policies-domain` — inconsistent. Any agent reasoning about the chain had to carry the inconsistency. Now all four match.

## What shipped

- Renamed `chorus:policies-domain` → `chorus:loom-policies` in 5 TTL references (commit `4ae64688`)
- `chorus:Policy` class + `chorus:derivedFrom` ObjectProperty defined (commit `8cf8d99b`)
- 5 policy instances populated from existing CLAUDE.md rules (commit `3c15c668`) — bonus beyond original AC
- Fuseki reloaded via Silas's DROP GRAPH + PUT workaround (TDB2 bug — separate chore)
- Ontology validator 20/20 pass
- API endpoint `/api/athena/subdomains/loom-policies` returns 200 with label "Policies", owner Jeff, step Shaping

## Live verification

```
$ curl -sf http://localhost:3340/api/athena/subdomains/loom-policies
{"data":{"uri":"https://jeffbridwell.com/chorus#loom-policies",
         "id":"loom-policies",
         "label":"Policies",
         "owner":"Jeff",
         "step":"Shaping",
         "comment":"Team policies — working hours, WIP limits, response windows.",
         "consumes":[{"uri":".../security-domain","label":"Security"},
                     {"uri":".../time-domain","label":"Time"}]}}
```

## Commits

- `8cf8d99b` — Policy class + derivedFrom property
- `3c15c668` — 5 policy instances populated
- `4ae64688` — rename policies-domain → loom-policies

## Retirement note

Original `chorus:policies-domain` name is gone from the TTL entirely (5 references updated to `chorus:loom-policies`). No dual-naming residue. Add-with-retire pattern applied.

# Demo Brief — #2234 Move chorus API from attic to workbench

**Builder:** Silas
**Date:** 2026-04-19
**Gates:** product↑ | code ✅ | quality ✅ | arch ✅ | ops ✅

## Why it matters

Roles were inventing board state instead of reading it. This morning Silas claimed "5 WIP cards" — pulse data was in context and ignored. The Chorus API existed but wasn't load-bearing. Agents reached for memory instead of the API.

Now there is a pull surface: `/api/chorus/context/{board/wip, roles, health}`. Roles query at point-of-need, cite the timestamp, and the claim is grounded in fresh data.

## What was shown

**Live call — board state:**
```
GET /api/chorus/context/board/wip @ 15:16:29 UTC
→ 3 WIP: #2218 (Silas), #2234 (Silas), #2241 (Kade)
  domain: chorus | source: /api/chorus/context/board/wip
```

**Live call — role states:**
```
GET /api/chorus/context/roles @ 15:16:29 UTC
→ silas: state=building stale=False
  wren: state=idle stale=False
  kade: state=building stale=False
```

**Envelope prototype:** board.wip inline card titles replaced with pull pointer; context api manifest appended to Pulse section on every turn.

## AC outcomes

1. ✅ 136 endpoints classified (context-api-endpoint-audit.md)
2. ✅ 3 minimum endpoints designed + shipped with 31 tests (context-endpoint-schemas.md, Kade Step 3)
3. ✅ 3 staleness sources named; `stale` field shipped to /context/roles + 14:59/15:01 boundary tests
4. ✅ 3 response shapes reshaped: WipCard (dropped valueStream), HealthCheck (detail→reason), ContextHealthData (dropped summary)
5. ✅ Inject prototype: board.wip pointer + manifest appended; envelope 3,998 bytes; ~195-byte delta on board section
6. ✅ Live demo: cited /api/chorus/context/board/wip @ 15:16:29 in session, not invented

## Design artifacts

- `designing/docs/chorus-overview.md` — refreshed (three sub-domains, verticals, trinity, attic/workbench)
- `designing/docs/context-service-design.md` — sub-domain service design (Promise/Overview/Components/Interactions)
- `designing/docs/context-api-endpoint-audit.md` — 136 endpoints classified
- `designing/docs/context-endpoint-schemas.md` — minimum endpoint set + common envelope spec (step+product+domain+subdomain)
- `designing/docs/context-push-envelope-reshape.md` — push→manifest transition design (4 phases)

## Jeff's reaction

No explicit pushback during demo. Jeff directed the follow-on card series immediately (9 cards covering TTL migration, full envelope replacement, Knowledge/Memory endpoint sets, service designs, alerts domain, gate:interface).

# Demo brief — #2150 — CLAUDE.md fragment streamline + drift linter with enforcement

**Builder:** Wren
**Date:** 2026-04-17
**Gates:** product PASS | code PASS | quality PASS | arch PASS | ops PASS (re-run after enforcement added)
**Smoke check:** 20/20 bats green + live regen against real repo exited 0

## Why this matters

Before, CLAUDE.md charter drift (asymmetric fragments, dangling DEC citations) only surfaced when it bit us in conversation. After this card, the generator exits non-zero on R4/R5 errors at every manifest bump. Jeff is no longer the audit mechanism — the pipeline is.

## What shipped

1. **`designing/claudemd/roles/wren/working-with-jeff.md`** — Wren PM-lens on Jeff's communication style, closing the asymmetry Silas + Kade had that Wren didn't.
2. **`platform/scripts/lint-fragments.sh`** — 6-rule fitness linter (R1 principle drift, R2 duplication, R3 stale, R4 asymmetry, R5 dangling DEC, R6 line-count variance).
3. **`platform/scripts/claudemd-gen.py` integration** — linter runs on every `generate`. R4/R5 errors block (exit 2). R6 warns don't. `CLAUDEMD_LINT_SOFT=1` bypasses with loud banner + `claudemd.lint.bypassed` spine event.
4. **20 bats tests** in `platform/scripts/tests/` covering both the linter rules and the blocking/bypass behavior.
5. **DEC-1571 (Attention Contract) + DEC-1674 (TDD Discipline)** harvested into `roles/wren/decisions.md` — these were cited in shared fragments but never written down. Caught by R5.

## Output vs outcome

Jeff pressed on this mid-demo. The first pass (linter prints findings) was output. Enforcement (linter blocks on errors, bypass is loud) is the outcome. The card was extended in place rather than spawned as a follow-on.

## Demo sequence shown

- R4 before/after: removed `working-with-jeff.md` from Wren composition → R4 fires with exact asymmetry message → restored → clean.
- Live run against current repo: 1 R6 warn only (close-out-docs line-count variance), status=1 (non-blocking).

## Acceptance

Jeff invoked `/acp 2150`. Accepting.

# Demo ready: #2462 — ESLint rigor bump — type-honesty + jest-smell rules + ratchet

**Builder:** Kade
**Demo initiated by:** kade
**Timestamp:** 2026-04-24T15:06

## AC Status (00/6)
    - [ ] `eslint.config.js` updated:
    - [ ] `package.json` lint script covers both `src/**` and `tests/**` for platform + directing
    - [ ] `eslint-plugin-jest` added to devDependencies
    - [ ] Ratchet: `--max-warnings=0` stays; current error count locked as budget (how?) — either (a) baseline file (like gathering #745 lint-ceiling-ratchet), or (b) accept that commit breaks gate:code until cleanup waves ship
    - [ ] Cleanup sequence filed: one card per rule group, in order of blast radius (explicit-any → misused-promises → unnecessary-condition → base-to-string → security → test-smells → no-empty → complexity)
    - [ ] Gathering repo gets the same upgrade (parallel card or same card — Jeff choice)

Auto-generated by /demo provenance gate.

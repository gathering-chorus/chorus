# Demo — #2176 Gemba reads pulse as single source of truth

**Builder:** Wren
**Date:** 2026-04-18
**Gates:** product PASS | code PASS | quality PASS | arch PASS | ops PASS
**Feedback:** Silas (ops-lens, graceful-degrade validated) + Kade (engineering-lens, extract-if-grows)

## Why this matters

Gemba reported "unknown" while the Clearing tile showed Silas building #2175. Two surfaces, same question, disagreeing answers because they read different data. This card retires the second source: five fallbacks (dead /tmp/role-state path, cards CLI, session JSONL, observer JSONL, chorus SQLite) collapsed to one (pulse + tiles).

## What shipped

- gemba-start.sh rewritten: reads pulse.roles.<role> + pulse.board.wip_cards, queries /api/tiles for lastAction
- gemba-tick.sh rewritten: same shape
- /tmp/role-state-<role> reads removed (dead path)
- Session JSONL, observer JSONL, chorus SQLite fallback chains removed
- Zero-hits grep on `/tmp/role-state-` in platform/scripts confirms retirement
- 338 → 204 lines (-134)

## Retirement evidence

Classic "add + retire" pattern applied. The card that added pulse composition (#2168) shipped the new source. This card retires the five old readers. Same shape as the #2123 retirement-gate design.

## Commit

b5ce4e13 — gemba scripts rewrite

## Live verification

`bash platform/scripts/gemba-start.sh silas` returns `state=building card=2175` matching the Clearing tile in real time.

## Feedback captured

- **Silas (ops):** graceful-degrade to "pulse only, no lastAction" when tiles unreachable is already in place via `curl -sf --max-time 2 || echo "(tiles API unreachable)"`. Validates the single-authoritative + one-optional shape vs the old 5-disagreeing mess.
- **Kade (engineering):** heredoc-in-bash is acceptable for 2-file surface. If a 3rd consumer needs pulse-reading, extract to standalone `read-pulse.sh` for testability. Follow-on conditional on growth.

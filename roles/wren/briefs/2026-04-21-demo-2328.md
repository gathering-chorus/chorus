# Demo: #2328 — ESLint max-depth/max-lines parity (gathering parity achieved)

**Builder:** Kade
**Status:** WIP → ready for accept
**Commits:** 23ae157a (wave 1, 5 handlers) + d20bc340 (wave 2, 8 files + budget drop)

## Why this matters
After #2288 closed the 29 `complexity` inline suppressions, the budget stayed at 7/274 because 23 pre-existing `max-depth` and `max-lines-per-function` violations hadn't been refactored. This card closes that gap — chorus lint now matches gathering end-to-end at `complexity 20 / max-depth 4 / max-lines 80`. No hidden budgets, no per-product drift, no suppression layer to forget.

## What I want to show Jeff
`npm run lint` clean at 4/80, zero warnings, zero suppressions. 23 functions decomposed into named helpers. 1986 tests green across api + clearing + cards + pulse + workflow-engine (pre-existing filter-command failure is the only red, unrelated).

## AC verification (6/6)
- [x] Budget dropped 7/274 → 4/80 (gathering parity); #2288 baseline comment removed
- [x] All 23 sites refactored — no `eslint-disable` added, mechanism is decomposition
- [x] `npm run lint` returns 0 errors / 0 warnings at 4/80
- [x] `gate-code-tests.sh --max-warnings=0` continues to enforce
- [x] No regression: 1299/1299 api, 280/280 clearing, 362/363 cards (filter pre-existing), 36/36 pulse, 9/9 workflow
- [x] Scope discipline: 23 sites, no suppressions, no re-raise, no surprise expansion

## Refactor summary by file
- `chorus-self.ts`: runFtsSearch + runSemanticSearch + runSparqlSearch
- `chorus-conversation.ts`: computeUtcISOForHour + computeWindow + isNoiseRow
- `chorus-reprompt-analytics.ts`: isNoiseContent + loadFilteredMessages + classifyEventType
- `chorus-domain-story.ts`: collectCardEntries + collectMentions + collectSpineEvents + isStoryNoise
- `athena-subdomain-completeness.ts`: buildSections + computeLifecycle
- `patterns-summary.ts`: recordPatternLine
- `discover-code.ts`: classifyEntry + makeScanDir + scanOverrideDir + writeDiscoveredInBatches
- `server.ts` discover-endpoints: buildHandlerToDomain + parseAppRoutes + writeEndpointsInBatches + module-level tables
- `pulse/service.ts`: buildMetrics + registerRequestLogging + registerHealthMetricsRoutes + 4 route-group registers
- `workflow-engine/engine.ts` advance: completeStep + unlockNextStep + writeHandoffBrief
- `clearing/server.ts` readRoleObservations: parseObservation + readObservationsForRole
- `clearing/session-tailer.ts`: newestJsonlIn
- `cards/sdk.ts` doneCard auto-unblock: tryAutoUnblockDownstream + maybeUnblockGated + allBlockersDone

## Follow-on
None required. This completes the lint-parity sequence (#2284 + #2288 + #2328).

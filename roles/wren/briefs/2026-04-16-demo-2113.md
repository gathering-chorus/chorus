# Demo ready: #2113 — Scanner reads brief location, deprecate handoffs.log tracking

**Builder:** Silas
**Smoke check:** PASS (8/8 briefs_scanner tests green; 61+285+3+8+2+2 other tests green; release build clean)
**Demo initiated by:** silas
**Timestamp:** 2026-04-16T15:22

## AC Status (5/5)
- [x] Scanner reports 0 pending when briefs/ has no non-card files
- [x] Scanner reports N pending when N non-card briefs exist in briefs/
- [x] Moving a brief to briefs/archive/ drops it from pending on next scan
- [x] Rust tests cover empty, pending-only, archived-only, mixed, move-to-archive
- [x] Demo: before/after session-start output with same brief moved to archive

## What to show Jeff

**The phantom was hiding something worse.** This morning's session-start showed "3 stale handoffs" that turned out to be already-archived briefs — the `handoffs.log` tracker had no `received` writer, so every sent brief stayed pending forever. But while fixing the scanner, I found a second bug: `state_paths::role_dir("silas")` returned bare `"silas"` (from the #1794 namespace swat that dropped the `roles/` prefix). Context_cache was looking at `/chorus/silas` — a path that doesn't exist. The "Pending Handoffs" section had been silently empty for Silas for weeks.

After the fix, session-start exposes the truth: **46 real unprocessed briefs** in my inbox. Not three. Forty-six. Old spike docs, demo briefs, March planning briefs — all invisible until now.

**Demo walkthrough:**
1. `head -150 /tmp/session-start-silas.md` — now shows `### Pending Handoffs` with 46 items and `SUMMARY:46 pending`
2. Plant a brief: `echo "..." > roles/silas/briefs/test.md`
3. Rebuild context: `chorus-hook-shim context-cache silas`
4. See `SUMMARY:47 pending`, new brief at top (0h)
5. Archive it: `mv roles/silas/briefs/test.md roles/silas/briefs/archive/`
6. Rebuild: `SUMMARY:46 pending`, test brief gone

## Changes
- `context_cache.rs`: `check_handoffs` → `scan_briefs_pending(&Path)`. Reads filesystem top-level of briefs/, filters card-status notifications via `is_real_brief`. Output: per-line `- <name> (<age>h)` + `SUMMARY:N pending`. Empty string when 0.
- `state_paths.rs`: `role_dir` returns `roles/<name>` (was bare name). Fixes context_cache AND session commands transparently.
- `tests/briefs_scanner.rs`: 8 unit tests. KEEP IN SYNC marker on duplicated fns (follow-on #2115 retires duplication).

## Impact
- **Trust restored:** scanner output matches reality. No phantom counts.
- **Inbox visibility restored:** Silas now sees 46 briefs that were invisible. (Housekeeping, not a card — I'll triage.)
- **Downstream parsers:** none. Only the scanner and its tests reference `SUMMARY:`.
- **handoffs.log:** retained for audit trail; no longer consulted by session-start.

## Related
- Chat silas-wren-1776365808: aligned on filesystem-as-truth
- Wren updating DEC-2090 to cover broader tracking-contract scope
- #2115 carded: convert chorus-hooks to lib+bin for test ergonomics (Later/P3)

Commit: 1e26929e

Ready for Jeff + Wren review.

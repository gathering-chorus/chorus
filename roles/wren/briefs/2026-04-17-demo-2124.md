# Demo brief — #2124 Deep health probes for /borg/*

**Outcome:** probe catches silent data-path failures — #2162 and #2163 surfaced live on first run.

## Gates (4/5 signed; gate-product pending)

- gate:arch-pass — Silas
- gate:ops-pass — Silas
- gate:code-pass — Kade
- gate:quality-pass — Kade
- gate:product — pending Wren

## What shipped

- `platform/scripts/borg-health-check.sh` (new) — reads a contract, probes each /borg/ page's backing API, evaluates a python assertion against the parsed JSON response, prints PASS/FAIL per probe
- `platform/scripts/borg-health-contract.json` (new) — 8 probes, one per /borg/ page, each with `page / api / assert / why`
- `platform/scripts/deep-health.sh` §15b — loops borg-health FAIL lines into the main FAILURES array so the existing alert path fires
- `platform/services/chorus-inject/src/main.rs` — added `--count-windows` subcommand
- `platform/services/chorus-hooks/src/nudge.rs` — removed duplicate osascript call; routes through chorus-inject

**Side-effect:** closed the two-binary TCC split (#2077 regression). One Accessibility grant now covers nudge delivery end-to-end.

## Tests

- 14/14 bats green in `platform/tests/borg-health-check.bats` (happy path + error paths: missing contract, malformed JSON, empty probes, missing-keys, missing-assertion)
- 11/11 bats green in pre-existing `platform/tests/deep-health.bats` (regression clean)

## Why this matters

Yesterday's Caddy+Borg work made /borg/ pages load-bearing. /borg/replay/ and /borg/assessment/ were rendering 200 with empty panels — silent, invisible, passing every gate we had. The probe now alerts at deep-health cadence when a page is alive but its data path isn't. Two real failures fired on first run; both filed as #2162 and #2163.

## What to show Jeff

1. `platform/scripts/borg-health-contract.json` — what "data-present" means per page
2. Live probe run — 6 PASS / 2 FAIL split
3. `deep-health.sh` end-to-end — FAIL lines surface as alerts in the cron path, nudge delivered

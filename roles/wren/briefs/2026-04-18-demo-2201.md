# Demo: #2201 — workflow-engine coverage 0% → 95.83%

**Builder:** Kade
**Date:** 2026-04-18
**Commits:** ef2fff24, c088ba39

## Gates

- gate:product — PASS (Wren)
- gate:code — PASS (Kade)
- gate:quality — PASS (Kade)
- gate:arch — PASS (Silas)
- gate:ops — PASS (Silas)

## Smoke

`npx jest --coverage` in `platform/workflow-engine`: 62 tests green, 2.5s. Thresholds 80/60/75/80; actuals 95.83 / 87.5 / 96.42 / 97.95.

## Why this matters

Coverage baseline was lying. workflow-engine reported 0% / 290 uncovered stmts — that's why #2201 was filed as "fastest win." Reality: 61 tests already existed, being silently bypassed because stray compiled `.js`/`.d.ts` files in `src/` shadowed the `.ts` at require-time. ts-jest never instrumented the real source.

## What was delivered

- Removed 12 shadow files from `platform/workflow-engine/src/` (from an old build with outDir=src).
- `jest.config.js`: excluded `src/cli.ts` and `src/index.ts` from coverage (bin script + pure re-exports).
- `tests/config.test.ts`: updated stale `architect/briefs` paths to `roles/<role>/briefs`.
- `tests/engine.test.ts`: +1 failure-branch test (`advance` throws with no active step — AC#2 "every failure branch has a test").

## Feedback received

- Silas (arch/ops): "Root-cause diagnosis is the real deliverable, numbers are the evidence."
- Wren (product): workflow-engine number now trustworthy. Opened follow-on audit under #2197 to scan other TS projects for the same shadow-file pattern — baseline integrity question.

## Result

TS aggregate coverage: 55.40% → 59.04%. Rust unchanged (66.1%).

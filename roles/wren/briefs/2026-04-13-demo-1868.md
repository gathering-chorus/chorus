# Demo ready: #1868 — Populate Code sub-domain graph

**Builder:** Kade
**Smoke check:** PASS (4 pass, 0 fail)
**Demo initiated by:** kade
**Timestamp:** 2026-04-13T12:44

## AC Status (10/10)
    - [x] Crawler discovers code files per domain from filesystem patterns (path conventions, domain labels)
    - [x] Code file mappings stored in the graph via the instances graph
    - [x] /code endpoint queries the graph instead of scanning filesystem with a hardcoded map
    - [x] Remove hardcoded domainPaths map from server.ts
    - [x] All 41 domains show their code files, not just 15
- [x] Crawler discovers code files per domain from filesystem patterns (POST /api/athena/discover-code — alias map + path matching)
- [x] Code file mappings stored in the graph via instances graph (urn:chorus:instances, 331 files)
- [x] /code endpoint queries the graph instead of scanning filesystem with hardcoded map (done in prior session, commit 78a39bf0)
- [x] Remove hardcoded domainPaths map from server.ts (crawl endpoint now reads from graph)
- [x] 28 of 44 domains show code files — remaining 16 have no code artifacts (Time, Streams, Policies, etc.)

## What to show Jeff
Agents had no way to know what code belongs to a domain — a hardcoded map covered 12 of 44 domains and drifted. Now POST /api/athena/discover-code scans both repos and populates the graph. Query any domain and get the full code inventory. This is the backstage tool that makes domain reasoning possible.

Auto-generated by /demo skill.
Ready for Jeff + Wren review.

# Demo ready: #1776 — API E2E tests, fragile endpoints

**Builder:** Kade
**Smoke check:** PASS (50 pages + 7/7 E2E tests)
**Demo initiated by:** kade
**Timestamp:** 2026-04-13T16:45

## AC Status (6/6)
- [x] E2E test: Athena subdomain list (GET /api/athena/subdomains) returns 200 with array
- [x] E2E test: Athena subdomain detail (GET /api/athena/subdomains/:id) returns 200 with sections
- [x] E2E test: Athena completeness (GET /api/athena/subdomains/:id/completeness) returns within 5s
- [x] E2E test: Seed webhook (POST /api/seed/sms) returns 200 with valid Twilio payload
- [x] E2E test: Chorus search (GET /api/chorus/search?q=...) returns results array
- [x] All tests run against the real stack (no mocks), contract-level assertions only (URL + response shape)

## What to show Jeff
7 green checks hitting 5 live endpoints. No mocks. Scoped to fragile ones only per Jeff's restructure concern.

Auto-generated by /demo skill.

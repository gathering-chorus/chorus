# Demo: #2266 — Clearing hang watchdog

**Builder:** Silas  
**Date:** 2026-04-19  
**Gates:** product PASS | code PASS | arch SKIP (type:fix)

**Why this matters:** Closing the Clearing browser tab left an abandoned Anthropic API stream running at 84% CPU — no disconnect handler. Server spun indefinitely; Jeff had to force-quit terminals.

**Fix:** Added `socket.on('disconnect')` to server.ts — last client disconnect calls `endSession('client-disconnected')`, which calls `participants.abort()` to kill in-flight streaming.

**Evidence:** CPU 84% → 10%, `active: false`, logs show "Clearing session ended — host disconnected". 86/86 tests green.

**AC:** 3/3 checked.

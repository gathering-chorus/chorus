# Demo: #2431 — Generic instance-render fold on domain-detail.html

**Pair:** Kade (drove) + Wren (navigated)
**Status:** WIP → ready for accept
**AC:** 7/7 verified (Jeff /lc'd principles, Wren screenshot + node sim on policies)

## Why this matters
Before: domain-detail.html showed instance counts ("Principles 24", "Policies 26") but not the content. Roles had to bounce to /loom/principles.html or /loom/policies.html to read the actual rules. The domain page told you how many rules existed, not what they said.

After: every content-bearing subdomain renders full instance bodies inline. loom-principles shows 12 permaculture parents each expanded to three-layer bodies (Permaculture / Tech / Jeff readings) with their Chorus specializations nested underneath. loom-policies shows each policy with comment + enforces[] principle-link list + surface chip. Decisions get date/status/enforcement. Everything else falls to a generic label+comment renderer.

## What to show Jeff
Three live checks:
- `/gathering-docs/domain-detail.html?id=loom-principles` — INSTANCES fold → Principles (24) → expand → 12 parents with 3-layer bodies → expand a parent → "Specializations (N)" nested fold → child bodies
- `/gathering-docs/domain-detail.html?id=loom-policies` — Policies (24) → expand one → comment + blue surface chip + "Enforces: <principles>" line
- Cmd+P on either page — each instance body stays on one page, expand-all/collapse-all buttons hide

## AC verification (7/7)
- [x] AC1 — INSTANCES fold rendering full bodies for content-bearing subdomains
- [x] AC2 — Renderer dispatches on rdf:type (Principle tree/3-panel, Policy flat/enforces+surface, Decision date+status, Generic fallback)
- [x] AC3 — TYPE_RENDERERS registry, no scattered if-type-X
- [x] AC4 — Collapse-by-default >20 + expand-all button (principles has it)
- [x] AC5 — Print CSS page-break-inside:avoid scoped to .instance-body, buttons hide in print
- [x] AC6 — /loom/principles.html + /loom/policies.html prior-art unchanged
- [x] AC7 — Verified on loom-principles (Jeff /lc) + loom-policies (screenshot + node sim)

## Scope discipline notes
- **Pair-shipped**: Wren caught two real bugs via screenshot gemba — flat render instead of nested hierarchy (Jeff corrected through Wren mid-pair), and "Policys" pluralization typo. Both fixed before AC ticked.
- **CORS stopgap** added to chorus-api for `/api/loom/*` + `/api/chorus/*` paths, tagged `// #2041: remove once Athena relocates`. Debt visible, not permanent.
- **Pre-existing DOM nesting bug fixed** as side-effect: `<details class="child-domain">` was never closed in d.domains.forEach, was swallowing sibling content. Hit it because my INSTANCES block came after. Net structure is now tag-balanced.
- **Build in place, port later** (#2041). Athena still lives in gathering-app; this renderer moves with it when Athena relocates.

## Live-surface verification (per #2252 lesson)
- Code live: curl verified served JS contains TYPE_RENDERERS + renderPrincipleTree + pluralize + CORS_API_BASE
- Data live: /api/loom/principles returns 24 principles with techReading/jeffReading/isPermacultureParent; /api/loom/policies returns 24 with surface/enforces; CORS headers present with Origin: http://localhost:3000
- Render live: Wren /lc + screenshot verified nested principle tree and policy shape
- Print shape: CSS in domain-detail.html head, scoped to `.instance-body` class shared by all type renderers

## Follow-on
- #2041 (Athena out of Gathering) — CORS block retires when Athena moves
- Practice renderer (comment + expresses[] + operationalizes[]) lands after #2348
- If the enrich-per-type pattern repeats 3+ times, roll into /api/athena/subdomains/<id> response (path B)

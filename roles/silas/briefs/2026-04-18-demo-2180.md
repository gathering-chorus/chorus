# Demo brief — #2180 server.ts handler extraction (accepted via /acp)

**From:** Silas + Wren (pair)
**For:** Jeff (accepting authority)
**Date:** 2026-04-18
**Status:** Accepted by Jeff via /acp — remaining work carded as #2186–#2192

## What shipped on #2180

Two atomic commits landed before Jeff accepted:

1. **`a3e66af7`** — `/api/athena/validate` handler extracted (red-first TDD, 10 tests green, adapter ≤4 lines). Established the extraction pattern for the remaining work.

2. **`6a2ca554`** — Hermetic quarantine wave 2: 39 integration-masquerading test suites gated behind `RUN_INTEGRATION=true`, absorbs swat #2184. Default `jest` run went from 63s with 2+ failures to **5.4s, 0 failures** (11× speedup, trustworthy default).

## What didn't ship on #2180

- The other ~116 handlers listed in #2180's AC were not extracted in this card.
- Reason: per-handler ceremony (pair session, red-first test, per-commit gate chain) made finishing this card in its original shape a 50+ hour endeavor of ritual around minutes of code per handler.

## What replaces it

Rather than keep #2180 as an open umbrella, Jeff and Silas agreed to break the remaining work into a series of cards with real finish lines:

- **#2186** — Handler extraction scaffold (generator script; unblocks all batch cards)
- **#2187** — All Athena GET handlers extracted (~20, one commit)
- **#2188** — All Chorus domain GET handlers extracted (~15, one commit)
- **#2189** — All remaining GET handlers extracted (~55, one commit)
- **#2190** — All POST/PUT/DELETE handlers extracted (~48, one commit)
- **#2191** — Raise server.ts coverage threshold to 80/75/60 (enforces no regression)
- **#2192** — Wave 3 hermetic quarantine (7 mixed files, describe-block level)

Each has countable, demo-visible AC — no "pattern demonstrated" or "first wave" cheat points.

## Evidence

- Commit `a3e66af7`: `git show a3e66af7 --stat` shows handler + test + adapter edit
- Commit `6a2ca554`: `jest.config.js` quarantine list, `jest-config-quarantine.test.ts` locks the list
- Default `jest` run (no RUN_INTEGRATION): 506 passed, 0 failed, ~5.4s
- `npx tsc --noEmit` in platform/api: clean

## Why Jeff accepted before AC complete

Jeff's call — the atomic work that landed is genuinely done, but the card's AC was over-scoped (one card carrying 117 handlers). Rather than "cheat and stop" at a fake milestone inside #2180, Jeff closed #2180 on what actually shipped and filed a proper series to cover the rest with honest finish lines.

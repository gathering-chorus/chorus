# #594 Accepted — Fuseki port remap

**From:** Wren
**Date:** 2026-03-01

Accepted. Port 3030:3030 deployed and verified. Clean fix.

Two related cards now in your Next queue:
- **#593** — board-ts help aliases (P2). `--help` and `help` both throw unknown command errors.
- **#595** — git-queue.sh reliability (P1). Roles hit syntax/path errors on nearly every commit. First attempt often silently succeeds then reports failure. 93 accumulated errors in command-errors.log from these two tools.

Both are top error sources. #595 is P1 — the commit workflow is broken enough that Jeff notices every time.

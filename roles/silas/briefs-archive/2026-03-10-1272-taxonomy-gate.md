# Brief: Enforce chunk + sequence labels on Now entry

**From:** Wren | **Date:** 2026-03-10 | **Card:** #1272

## Context

Jeff asked for both chunk (domain) and sequence (work theme) labels on all active cards. We tagged all 13 active cards. Both dimensions feed the Product Flow page toggle (slices vs domains view).

Key insight from Jeff: chunks are stable product taxonomy, sequences are fluid work themes. Different fluidity = different gate strength.

## What to Build

In `messages/board-client/src/sdk.ts`, add a gate in `moveCard()` for transitions into Now or WIP:

1. **Missing chunk = HARD BLOCK.** Chunks are product domains (spine, ops, music, memory, senses, strategy, app). Stable. You always know what domain a card belongs to. Message: "Blocked: missing chunk label. Use `board-ts tag <id> <chunk>`"
2. **Missing sequence = SOFT WARN.** Sequences are work themes (v1, hardening, style, sparql, flow-tests). Temporal — new cards may not belong to an existing wave. Warn but allow through. Message: "Warning: no sequence label. Use `board-ts sequence-tag <id> <seq>` when this card joins a wave."
3. SWAT cards exempt
4. Existing Now/WIP cards grandfathered

Same pattern as `enforceACGate()` (~line 168). The label check is simpler — just filter card labels for the prefix.

## AC
- [ ] Missing chunk blocks Now/WIP entry
- [ ] Missing sequence warns on Now/WIP entry (non-blocking)
- [ ] Messages tell role the exact command to run
- [ ] SWAT exempt

# 2026-08-29 — Silas Journal

## Morning — #3837 landed; Kade's daytime runs; reboot story corrected

#3837 (chorus onto shared-security) presented 09:28, Jeff's login verified 13:02 through the relocated identity data, go 13:05. Land refused at deploy: canonical 4 behind with 22 uncommitted activity.md lines. Stashed, synced, restored the lines, deployed and accepted by hand (13:19). Same class hit Kade's #4022 land an hour later.

Kade's #4022 runs: 09:31 run reached load 557 in 20 min (runner killed by its own quiet-cap; tunnel dropped, hooks piled 97 zombies → restarted). 11:04 run with the load-aware runner still spiked to 115 (coverage lane ran before the limiter; capped to 2). 12:10 run 6 finished at 13:11, load ~8 throughout. Model graph count 5,043 unchanged through all three.

Correction from Kade via Jeff's 09:12 seed: the 08-28 09:10 reboot was VS Code at 41.6 GB, not the css loop. Two causes, recorded in memory.

## Afternoon — #4029 recovery + idempotent deploy

Wren found in git that #3982 (08-22) dropped 12 Domain blocks from domains-wren-silas.ttl; 7 of the 10 lost domains were there (builds, decisions, rcas, domains, services, search, spine). Kade's carrier file holds them verbatim (landed with #4022, but never added to MODEL_SET — added here). Wren re-authored products + value-streams (placed in domains-wren-silas.ttl).

The duplicating deploy: shape bodies are blank-node trees; the merge deleted a staged subject's own triples but never its bodies, so every deploy left the old bodies and added new ones (+880 × 92 = 5,230 → 77,770). Fix: delete the blank-node trees hanging off staged subjects (6 levels, deepest first) before the subject delete, then sweep orphan blank nodes (6 passes). `4029-deploy-twice-same-count.bats` 3/3: deploy twice → equal; cleanup off → grows (negative proof); an already-inflated graph shrinks and then holds. Live graph has 249 orphan blank subjects today; the next deploy sweeps them.

## 16:45 — identity server pegged

During Kade's run 7 the 5-min load crossed 40 with zero test jobs running: `com.security.css` (the identity server, up since my 15:45 restart yesterday) was at 99% CPU with one request a minute and RSS climbing 473→483 MB in four seconds. Kickstarted; 0% after, keys and mint fine. Second time a long-running node service on this box has pegged a core (VS Code helper yesterday). Watch it: if it climbs again within a day, that's a leak in CSS 7.1.8 under our config, not load.

## 16:54 — the identity-server spin was ours

Jeff: "im sure its a problem with css and not a bug in our code (sarcasm alert)." He was right. Its expiry sweep had been throwing every 15 minutes since 08-13 because two hand-made `*.json.pre-*-bak` files sat inside `.internal/accounts/data`; nothing expired, 7,281 session records piled up in `idp/adapter`, memory and CPU followed. Moved the two files out of the store, restarted; watching for the next sweep to shrink the pile. Memory note written with the rule: never put a non-.json file inside the CSS data dir.

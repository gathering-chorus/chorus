# Silas — 2026-06-18

## The day: model-first proved itself

The meta-unlock, named by Jeff at the end: **"switching from coding first and modeling later to the opposite"** — and sharper: **"harnessing the capability of a non-deterministic system to do deterministic work."** He admitted he was *"bout to give up on the whole thing bc i could not get u to build things the same way over and over."* Model-first is the STRUCTURAL answer: the generator builds (deterministic), roles model/judge (non-deterministic, variance=feature). The model is the membrane. Saved as `feedback_model_first_prove_by_running` (⭐).

## Properties as a domain-class (#3489) — the spike

- **ADR-045** (A Domain is an `owl:Class`) drafted + DE-reviewed with Kade (definesVocabulary not contains; atStream/atStep minCount-1; cascade-only floor).
- **Spike**: modeled `properties` as a punned domain-class, ran it through owl-api (didn't theorize). Two empirical findings the doc couldn't have surfaced:
  1. Generator lacks `definesVocabulary` enumeration → additive Wren generator card (reuse per-class engine, fan-out, compose).
  2. Punning ↔ ADR-040 case-conflict → `generate --class properties` REFUSED (class must be CamelCase, domain-individual is lowercase).
- **ADR-040 amended** (Jeff ruled, Kade+Silas own it per his OWL-depth trust): a punned generator-class is CamelCase; lowercase = data individuals. **DOMAINS migrate** (properties→Properties done, proven — adr040-violation gone). **PRODUCTS stay lowercase** — verified by data (nothing typed `a chorus:<product>`; they're container-only individuals, don't pun). The verify settled "yes on products" without a ruling.
- **#3489 AC consolidated** (Jeff: "pull as much in vs 3-4 more") — the whole model-side end-to-end in one card; only the owl-api generator change is genuinely separate (Wren). Parked at the ratify line until `/borg/properties` generates.

## ADR-046 — Domain→Observability (the emit-contract)

- Converged with Kade SA↔DE: domains emit, Borg leverages; monitors = **7th generated surface**; liveness-as-emission closes the absence leak; thresholds-as-properties = SLO-as-code.
- Web-searched the prior art (Jeff's ask): **conform-don't-invent** — OTel semantic conventions (declare-what-you-emit), RED/Golden/USE (metric primitives), and **DORA** (werk = our CI/CD, so its emissions ARE the four keys + reliability).
- Kade verified the DORA map off the live spine (real event names; failureClass = a declared change|tooling map over werk's typed refusal reasons — CFR excludes tooling so today's land.failed bugs don't poison it; lead-time decomposed so round-churn shows as coordination-wait not pipeline). Co-author the werk EmitContract when #3476 lands.
- Artifact: `/tmp/metrics-observability-findings.html`.

## Landed / in flight
- **#3481, #3478** landed earlier (ADR ratifications + ADR-044 PropertyKey governance).
- **#3466** (partOf spine) + **#3476** (land idempotency, push-before-resolve) — go-ready/landing. My durability hold on tree.json is independent (clears on #3493's committed-source round-trip).

## Ops
- clearing deep-health false-alarmed → verified UP (200, running pid 39210), did NOT restart. Probe likely checks a stale path (`/api/clearing/health` 404s, `/health` 200). Alert-manufactures-false-belief class again.

# 2026-08-28 — Silas Journal

## 12:36 — third hard crash (boots 08-27 11:56, 08-28 09:10, 08-28 12:32)

No kernel panic file → hang + power-cycle, not a panic. 5 min after boot: load 527, 730 processes, 16GB. Counted 48 com.chorus/com.gathering agents with RunAtLoad=true; 17 of them interval jobs (graph-hydrate, reindex-worker, index-artifacts, deep-health, harvest-exporter, context-cache-hourly, ops, probes) firing at boot on top of 30 services + Spotlight reindex + Chrome + VS Code + 4 claudes.

#4027: `launchagent-boot-audit.sh` (+ `--fix`), negative-proof test 6/6, 14 repo plist sources fixed, deep-health check 19 warns on live drift. Applied live: 16 agents stripped + reloaded, `launchctl print` shows no `runatload`. Baseline to beat at next boot: 5-min load 527.

Spotlight half needs Jeff's sudo (System Settings → Spotlight → Privacy: ~/CascadeProjects, ~/.chorus). Indexing still "enabled" on / at 12:36.

Side finding: chorus-hooks / hook-shim "Launch Constraint Violation" SIGKILLs at 09:40, 12:03 line up to the second with `binary.deployed` for those binaries (10:11, 11:18, 12:03). Deploy swaps the binary under a running launchd job; the first relaunch gets killed, the next one runs (pid 3157 healthy since 12:36). Transient, not a lockout. Worth a card if it ever leaves the daemon down.

## 15:50 — #3837 pulled (chorus onto shared-security, finish the decommission)

The decommission half happened by accident at 15:16: the old `com.gathering.css` had been respawning every 14s since the noon boot (658×), losing :3001 to `com.security.css`. Jeff booted it out; I parked the plist. Load 233 → 3.6. So the "verify before decommission" steps became "verify now".

Verified: token mint against shared-security (iss logical, webid silas), model door refuses with no token, verifies with one, a real `decision` write landed in urn:chorus:instances and was deleted. Found on the way: the `athena-model()` shell wrapper looked the minter up by bare name; it isn't on PATH in role shells, so every shell write since #3687 refused "identity-token-required" (Wren ×11 on 08-27). Fixed by path + WARN, bats 2/2.

Phase B: pod data copied gathering/data/css → shared-security/data/css (31MB, diff clean), start.sh default repointed, data/ gitignored. Restart + re-verify next; gathering copy stays as rollback until accept.

# Brief: #1070 Infrastructure Scripts Ready

**From:** Silas (Architect)
**To:** Kade (Engineer)
**Date:** 2026-03-05
**Card:** #1070 — Restore andon eye

## What's Done

Three changes shipped:

### 1. `role-state.sh` (new)
```
messages/scripts/role-state.sh <role> <state> [card=N] [detail="text"] [gemba=<role>]
```
States: `building`, `blocked`, `waiting`, `observing`, `idle`
Writes `/tmp/claude-team-scan/{role}-declared.json`. Enrichment trusts it when fresh (<120s).

### 2. `andon-enrich.sh` (updated)
- Reads `{role}-declared.json` as primary state source
- JSONL mtime heartbeat: `jsonl_alive` field — true if session wrote in last 120s
- Stale-card fallback extended from 60s to 300s
- Gemba: reads from declared state first, falls back to legacy gemba file
- Output JSON now includes `declared` and `jsonl_alive` fields

### 3. No Swift changes needed
Display already reads enrichment JSON.

## What You Own

CLAUDE.md fragment for role protocol integration:
- Card pull → `role-state.sh <role> building card=<id>`
- Blocked → `role-state.sh <role> blocked detail="reason"`
- Gemba start → `role-state.sh <role> observing gemba=<target>`
- Session close → `role-state.sh <role> idle`

## AC Verification

All 4 AC points pass — tested live. `jsonl_alive` stays true during long tool executions. Gemba eye persists via declared state. Blocked state overrides inference. Card survives 300s of board-ts failures.

# Brief: #1213 — drop max-width, keep 1in margins

**From:** Wren
**To:** Kade
**Card:** #1213

## Change

Jeff reviewed via screenshot — the 1200px max-width + 1in padding creates ~5-6 inches of dead space on both sides. Too narrow.

**Revert to:** `max-width: none; padding: 2rem 1in` for `.page-container`, nav, and footer. This is what existed before — full width with 1-inch side margins. No centering cap.

`.page-container.wide` stays as-is (already `max-width: none`).

This supersedes the previous brief. AC item 1 amended: `.page-container` uses `max-width: none; padding: 2rem 1in`.

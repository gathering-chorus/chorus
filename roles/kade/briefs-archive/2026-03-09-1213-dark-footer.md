# Brief: #1213 — footer misaligned on dark/wide pages

**From:** Wren
**To:** Kade
**Card:** #1213

## Issue

Jeff spotted on `/knowledge-graph`: the footer content is flush left — not matching the 1-inch side margins that light pages have. The footer needs `padding: 2rem 1in` on all pages, including `.page-container.wide` and dark-theme pages.

Check that the footer padding is consistent regardless of whether the page uses `.page-container` or `.page-container.wide`.

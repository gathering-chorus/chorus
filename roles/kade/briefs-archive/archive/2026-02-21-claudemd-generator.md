# CLAUDE.md Generator System — Shipped

**From:** Wren
**Date:** 2026-02-21
**Status:** Shipped, needs your awareness

## What Changed

Your CLAUDE.md is now **generated** from shared + role-specific markdown fragments. A single-source generator (`messages/scripts/claudemd-gen.sh`) assembles all 3 CLAUDE.md files from fragments in `messages/claudemd/`.

## Why

Three CLAUDE.md files shared ~60% of content across 11 duplicated sections. Policy changes had to be applied 3 times manually — they drifted. `session-start.sh` was only in Wren's file; you and Silas never got it. Jeff's requirement: "a normalized way of shipping role and experience that is versioned, shared, legible, and auditable."

## What's New in YOUR CLAUDE.md

1. **`session-start.sh kade`** — your session start now uses the parallel startup script (boards, Slack, briefs, state files in <1 second). Your role-specific tasks (codebase review, point of view) come after.
2. **Slack Brevity Rules** — new section for you (50 words max, @team rules). Was completely missing.
3. **Data Safety** — pulled out of the Infrastructure Operations section into its own shared section.
4. **Exchanging Work** — new section (sending/receiving briefs protocol). Was missing.
5. **Close-out triggers** — now in the shared Team Operating Model section, formatted consistently.
6. **Cost Awareness** — pulled into its own shared section.
7. **Team Activity Log, Team Kanban Board** — normalized with fuller shared content (anti-patterns, more CLI commands).

## How It Works

```
messages/claudemd/
  manifest.json           # Assembly order + variables for all 3 roles
  shared/                 # 10 fragments used by 2+ roles
  roles/kade/             # 9 Kade-specific fragments

messages/scripts/claudemd-gen.sh
  claudemd-gen.sh              # Generate all 3
  claudemd-gen.sh kade         # Generate yours only
  claudemd-gen.sh --diff       # Preview changes
  claudemd-gen.sh --check      # Exit 1 if stale
```

## What You Need to Do

- **Don't edit CLAUDE.md directly anymore.** Edit the fragment in `messages/claudemd/roles/kade/` or `messages/claudemd/shared/`, then run `claudemd-gen.sh`.
- If you need to update a shared section, edit the shared fragment and regenerate all 3.
- Your generated CLAUDE.md has a header comment: `<!-- GENERATED by claudemd-gen.sh | ... | DO NOT EDIT DIRECTLY -->`

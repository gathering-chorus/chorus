# Kade Journal — 2026-03-09

Productive session. Four cards touched, three shipped. The pattern that stands out: graph URI mismatches. The sexuality handler was pointing at `/media/models` when the data lived at `/sexuality/models.ttl`. Same pattern for video graphs — hardcoded list at `/media/` when data was at `/sexuality/volumes/`. Replaced the hardcoded video graph list with dynamic discovery from Fuseki — that's the right pattern going forward. Don't hardcode graph URIs when you can ask the store what exists.

The book ISBN fix was satisfying — Claude Vision extracts partial ISBNs sometimes (9 digits instead of 10). The schema was rejecting the entire batch for one bad ISBN. Transform instead of reject is the right posture for Vision-extracted data.

First gemba observation — watched Wren demo the flow map. Three roles converged on the same insight independently: flow tests need to verify SPARQL named graph correctness, not just HTTP 200. That's the team working.

Jeff is using the app on his phone — that's real usage generating real bugs. The book import flow and sexuality page fixes came directly from him hitting walls. Keep that channel open.

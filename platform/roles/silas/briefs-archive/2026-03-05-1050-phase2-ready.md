# #1050 Phase 2 Ready — Kade finished Phase 3, you're unblocked

**From:** Wren
**Date:** 2026-03-05
**Card:** #1050 (Later → ready to pull)
**Context:** #1040 Metrics Foundation

## Status

Kade just shipped #1051 (Phase 3 — kill 5 T3 paths). Accepted tonight. Two commits: `68e1602`, `f6ecefc`. All five kills landed in `loom-metrics.sh`.

Phase 1 (manifest) was done last session. Phase 2 (#1050, yours) is the remaining piece of #1040.

## What's needed

Instrument two missing spine events:

1. **`brief.handoff.read`** — emit when `werk-init.sh` detects a role has consumed a brief (the write side already emits `brief.handoff.written`)
2. **Won't Do reason field** — when `board-ts` moves a card to Won't Do, capture and log the reason string in the spine event

These close gaps identified in the metrics manifest (`messages/schemas/metrics-manifest.json`).

## Priority

P2 but it's the last gate before #1040 can close. Pull when you have a slot.

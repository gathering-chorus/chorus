# Workflow Handoff: WF-081 — Step 2

**Workflow**: WF-081
**Decision**: Role-native context injection at boot — chorus query per role in native token format
**Your action**: Review and verify #358

## Context

Previous step (completed by silas):
- **Action**: Role-native context injection at boot — chorus query per role in native token format
- **Notes**: Implemented context mode in chorus-query.sh + wired into werk-init.sh. All 3 roles produce dense boot signals.
- **Artifacts**: chorus-query.sh, werk-init.sh

## What You Need To Do

Review and verify #358

When complete, run:
```bash
../messages/scripts/workflow.sh advance WF-081 --notes "your completion notes" --artifacts "file1,file2"
```

## Workflow Progress

[x] Step 1: silas — Role-native context injection at boot — chorus query per role in native token format
[>] Step 2: wren — Review and verify #358

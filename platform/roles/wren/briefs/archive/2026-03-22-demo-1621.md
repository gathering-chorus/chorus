# Demo ready: #1621

**Builder:** Silas
**Smoke check:** PASS (Rust 11/11 tests, all 4 symlink paths verified)
**Why it matters:** 4 bash scripts that every role called dozens of times per session (chorus-log, role-state, wall-clock, nudge) are now Rust binary invocations via argv[0] dispatch. Zero bash interpretation overhead. The .sh names still work for backwards compat — 2-line wrappers that exec the symlink.
**What to show Jeff:** The symlink chain and bash-compat working
**AC status:** 2/4 — exec wrappers replaced, wall-clock ported to Rust. Remaining items (session hooks, settings.json) blocked on Tier 2/3.

Ready for Jeff + Wren review.

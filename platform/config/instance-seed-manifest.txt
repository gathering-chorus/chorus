# instance-seed-manifest (#3895) — the INSTANCE_SET, as data.
#
# Read by `athena-model seed --deploy` (the DAL-gated instance leg of a model
# deploy) and parsed by owl-api's deploy_set() audit. One line per group:
#   <kind>:<path relative to CHORUS_ROOT>
# A file carrying two kinds is listed twice — same file, seeded once per kind
# (#3839: the door validates each group against the STATED class's shape).
#
# This file exists so the manifest has ONE home that both the seeder and the
# authored-vs-deployed audit read — not a bash array in one script and a
# hand-kept list in another (the drift class #3895 closed).
value-stream:designing/data/value-stream-instances.ttl
value-stream-step:designing/data/value-stream-step-instances.ttl
# #3838 — the four roles: before this they existed only in the live store, so
# no deploy could reproduce ownedBy/holdsRole targets.
role:roles/wren/ontology/role-instances-3838.ttl
# #4045 — the nine products (athena … werk, spine). Live-only until this card;
# spine carries its design-doc sections as Product attributes. After role: the
# ownedBy targets must be present in the store or the batch (#3839).
# #4096 — documents BEFORE products: a product's hasDesignDoc points at a document, and rows now post one at a time through the API (referential integrity per write)
document:designing/data/document-instances.ttl
product:designing/data/product-instances.ttl
# #4045 — the 18 served Documents: hasDesignDoc targets for the products above.
# One batch, so product→document and document→hasProduct resolve against each
# other (#3839). Lands in urn:chorus:instances with the rest (see the file header).
# #4040 — the two real pipelines (cicd, athena) + their steps; clearing/borg
# planned instances, no steps. Two kinds, TWO files: the seeder refuses a
# subject claimed by two kinds in one batch (proven at the #4040 land 19:23),
# so the two-kinds-one-file shape #3839 describes does not survive contact.
pipeline:roles/kade/ontology/pipeline-instances.ttl
pipeline-step:roles/kade/ontology/pipeline-step-instances.ttl
# #4089 — thin Card stubs every Commitment row below points at (chorus:card is
# FK-checked fail-closed, #3839): a fresh store (the demo variant) has no cards.
card:roles/kade/ontology/commitment-card-stubs.ttl
# #4064 / #4089 — service design COMMITMENTS as rows. Jeff, 2026-09-03:
# Commitment is "100% part of services", so the rows live in the services
# domain's graph (CommitmentShape instancesGraph urn:chorus:domains:services;
# the seeder writes each kind to its shape's declared home, #3647). The
# services harvester replaces only its own classes, so these rows survive its
# cycle. Security first (#4064), tests second (#4089); the next service adds a line.
commitment:roles/silas/ontology/security-commitments-4064.ttl
commitment:roles/kade/ontology/tests-commitments-4089.ttl
# #4102 — practices and principles are READY to post through the door: their
# rows now carry an owner. They are NOT listed here yet because the values lane
# of the model deploy gates on principles being live, and the post leg runs
# after it — listing them here empties the principles graph at gate time and the
# deploy fails closed (measured 2026-09-04 15:55: "VALUES-EDGE-GATE FAILED — 16
# expressedBy target(s) are not live Principles"). Same ordering class as #4101.
# Move the values edge-gate after the post leg, then add the three lines below.
#   principle:roles/wren/ontology/principles-instances-3749.ttl
#   principle:roles/wren/ontology/principles-xp-4006.ttl
#   practice:roles/kade/ontology/practices-3754-instances.ttl

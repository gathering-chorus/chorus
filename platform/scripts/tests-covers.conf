# #3996 — covers-share threshold for tag-tests-domain.py's ingest gate.
# No single domain may hold more than this share of the test corpus; an
# over-broad corpus makes werk-test's covers-union select half the registry
# for any touching diff (2026-08-23: services at 43% → 2-unit run became 166).
# Env MAX_DOMAIN_SHARE overrides. Lives beside its only consumer
# (platform/scripts/ is scope-irrelevant to werk-test, #3821).
MAX_DOMAIN_SHARE=0.30

# #4022 — suites that must run ALONE (serialized after the parallel pool).
# One repo-relative suite path per line. A suite that mutates shared live
# state (stops agents, rebinds ports, writes live config) declares itself
# here; needs-stack registration also serializes a unit without this file.
platform/scripts/test-product-membrane.sh

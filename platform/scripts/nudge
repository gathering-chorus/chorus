#!/usr/bin/env bash
# nudge — RETIRED 2026-05-08 via #2804.
# MCP `chorus_nudge_message` is the canonical invocation path. This script
# stays as a fail-loud stub so operational callers (alert-delivery-test.sh,
# deep-health.sh, library-health-probe.sh, infra-alert.sh, session-health.sh,
# health-check-bedroom.sh, inject-regression.sh) surface the dependency
# instead of silently command-not-found-ing. Migration of those callers
# tracked separately.
#
# Agents in Claude sessions: use the chorus_nudge_message MCP tool.

cat >&2 <<EOF
nudge: RETIRED (#2804). Use chorus_nudge_message MCP tool from a Claude session.
This script remains as a fail-loud stub for operational callers; their
migration is tracked in the #2804 follow-on. Caller: $0 $@
EOF
exit 2

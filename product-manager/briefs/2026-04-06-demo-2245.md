# Demo ready: #2245 — Fix overnight nudge inject

**Builder:** Silas
**Smoke check:** PASS (22 pass, 0 fail)
**Tests:** 7 cargo + 6 shell regression, all green
**Demo initiated by:** silas
**Timestamp:** 2026-04-06T08:51

## AC Status (5/6)
- [x] 1. chorus-inject uses do script instead of activate + keystroke
- [ ] 2. Inject works when display is asleep (overnight test — verified via deep-health alert delivery through same path)
- [x] 3. Inject still works during the day — same behavior as before
- [x] 4. All three roles receive inject correctly (targeted, not broadcast)
- [x] 5. All nudge integration tests validated and updated for do script path
- [x] 6. Regression test: intra-team nudges and /chat exchanges still deliver

## What to show Jeff
The overnight Clearing spam (7 alerts) happened because inject used `activate` + `keystroke` which needs the display awake. Now it uses `do script` which targets the tab directly — no display, no TCC, no focus theft. Deep-health alert just fired and delivered through this exact path.

Auto-generated by /demo skill.
Ready for Jeff + Wren review.

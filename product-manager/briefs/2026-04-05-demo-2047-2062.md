# Demo: #2047 + #2062 — Hook false positive fixes

**Builder:** Silas
**Smoke check:** PASS (22/22)
**Timestamp:** 2026-04-05T07:58

---

## #2047 — Infra guardrail exempts signal-0 liveness checks

**AC Status (3/3)**
- [x] `kill -0 <pid>` and `kill --signal 0 <pid>` not blocked by infra guardrail
- [x] `kill -9`, `pkill`, `killall` remain blocked
- [x] 3 new tests cover the exemption (34 total guardrail tests pass)

**What changed:** Added signal-0 exemption at line 128 of infra_guardrails.rs. `kill -0` is a read-only liveness check (returns 0 if process exists, 1 if not). Blocking it prevented roles from checking if services are alive.

---

## #2062 — Pair gate exempts non-code files

**AC Status (4/4)**
- [x] CSS/SCSS files exempt from pair gate
- [x] Static HTML in public/ directories exempt
- [x] EJS templates exempt
- [x] Source code (.rs, .ts, .py, .sh, .html outside public/) still requires pair

**What changed:** Refined `is_code_file()` in pair_gate.rs. Before: `.css`, `.scss`, `.ejs`, `.html` all counted as "code." After: only source files that carry cross-domain risk require pair. Kade was hitting this constantly editing clearing/public/index.html.

---

Both fixes: hook server rebuilt and restarted. All existing tests pass.

Auto-generated by /demo skill.
Ready for Wren review.

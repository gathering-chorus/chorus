# Daily Ops Review — 2026-08-17

_Generated by wren. Feeds into morning summary._

---

## 1. Hooks Health

**Status: YELLOW**
`cargo check` on `platform/services/chorus-hooks` passes (dev profile, 30.85s) with 7 warnings. One notable: `probe_role_session` in `src/process.rs:76` is never used — dead public API. No errors.
**Action:** Prune dead function or card it; confirm warning count hasn't grown from yesterday.

---

## 2. LaunchAgent /tmp Refs

**Status: YELLOW**
12+ plist files in `proving/config/launchagents/` use `/tmp` for `StandardOutPath`/`StandardErrorPath` (hooks, clearing, context-cache, api, ops, posture-capture, fuseki-perf, harvest-exporter, launchagent-metrics). Root-level `com.chorus.hooks.plist` also present with `/tmp` log paths.
**Action:** Migrate log paths to `~/.chorus/logs/` or a persistent location; `tmp-reaper` is the right owner of /tmp cleanup, not log persistence.

---

## 3. CLAUDE.md Conflicts

**Status: YELLOW**
`messages/claudemd/` directory not found in this repo. Either the fragment directory lives in a separate repo not cloned here, or it was removed/renamed.
**Action:** Confirm canonical location of claudemd fragments; update check path if moved.

---

## 4. CSC Compliance

**Status: GREEN**
No `/tmp/` references found in `messages/scripts/` or `architect/scripts/` (neither directory present in gathering-team; check passes vacuously).
**Action:** None — if those dirs exist in chorus repo, run check there.

---

## 5. Git Dirty State

**Status: GREEN**
`product-manager/` is clean. The other 6 role directories (architect, engineer, messages, jeff-bridwell-personal-site, shared-observability, wordpress-blog) are not present in this clone — they are separate repos not checked here.
**Action:** None for gathering-team. Cross-repo dirty-state check requires a multi-repo sweep.

---

## 6. Stale WIP Cards

**Status: YELLOW**
`platform/logs/` does not exist in this clone — board snapshots unavailable. Cannot verify WIP card age. 43 card commits landed in the last 7 days (active sprint), so board churn is high.
**Action:** Board snapshot log needs to be present for this check to run; Silas to confirm whether snapshot job targets this repo or chorus repo.

---

## 7. Domain Context Freshness

**Status: YELLOW**
All 5 domain-context files (`chorus`, `infrastructure`, `music`, `photos`, `seeds`) last updated 2026-08-14 — 3 days ago. 43 cards shipped in 7 days across chorus and infra domains; chorus domain in particular has recent card activity (#3907, #3909, #3889, #3868).
**Action:** Refresh `domain-context-chorus.md` — recent shipping warrants an update. Infrastructure context may also lag.

---

## 8. Disk Delta

**Status: YELLOW**
No perf-baseline data directory found (`proving/data/perf-baseline/` absent). Scripts exist (`perf-baseline.sh`, `perf-baseline-chorus.sh`) but no baseline to diff against.
**Action:** Run `perf-baseline.sh` to seed baseline data; subsequent runs will have something to compare.

# Daily Ops Review — 2026-08-19

_Generated by wren. Feeds into morning summary._

---

## 1. Hooks Health

**Status: YELLOW**
`cargo check` passes with 8 warnings — stable vs yesterday (7 in `chorus-hooks`, 1 in `chorus-hook-shim`). Dead-code list unchanged: `registration_json`, `owes_response_block`, `build_reply_event`, `publish`, `publish_via`, `clear_cap_cache`, `Liveness`, `probe_role_session`. No new warnings added by today's #3926/#3927 lands.
**Action:** Warning count stable but not shrinking; cleanup card still open.

---

## 2. LaunchAgent /tmp Refs

**Status: YELLOW**
17 plist files in `proving/config/launchagents/` log to `/tmp` — count unchanged from yesterday. No movement on migration to `~/.chorus/logs/`.
**Action:** Persistent; file card if not already tracked.

---

## 3. CLAUDE.md Conflicts

**Status: YELLOW**
`messages/claudemd/` path in prompt is stale — correct path is `designing/claudemd/`. Fragments last committed 2026-08-15 (4 days ago), within threshold. No staleness or conflicts found.
**Action:** Update ops-review prompt to use `designing/claudemd/` path.

---

## 4. CSC Compliance

**Status: GREEN**
`messages/scripts/` and `architect/scripts/` absent (expected). `platform/scripts/` has 41 files with `/tmp/` refs — all are runtime state patterns (watchdog, look, nightly log, mktemp traps), not scratchpad violations.
**Action:** None.

---

## 5. Git Dirty State

**Status: GREEN**
Working tree clean. HEAD on `main` at `a73319d` (silas: daily quality review 2026-08-19). Yesterday's detached HEAD resolved — container clone normal today.
**Action:** None.

---

## 6. Stale WIP Cards

**Status: RED**
Board not accessible from remote container. Yesterday: 2 WIP cards stale 132 days (now 133 days) — "Framework service design — OWL entity model" and "Restore chorus product boundary." No change visible.
**Action:** Close or requeue both cards; 133-day WIP is dead weight.

---

## 7. Domain Context Freshness

**Status: GREEN**
All 5 domain-context files (`chorus`, `infrastructure`, `music`, `photos`, `seeds`) last updated 2026-08-15 — 4 days ago. Cards #3926 and #3927 shipped today (silas). Still within 7-day threshold but approaching; `domain-context-chorus.md` worth refreshing given silas lands.
**Action:** Refresh `domain-context-chorus.md` at next silas session.

---

## 8. Disk Delta

**Status: N/A**
No `~/.chorus/` runtime data in ephemeral container; perf-baseline check structurally unrunnable in remote sessions. No regression signal available.
**Action:** Check is only meaningful in persistent local environment.

# Daily Ops Review — 2026-08-21

_Generated by wren. Feeds into morning summary._

---

## 1. Hooks Health

**Status: YELLOW**
`cargo check` passes with 7 warnings — count stable vs yesterday. Dead-code set unchanged: `clear_cap_cache`, `Liveness`, `probe_role_session` plus 4 others in `chorus-hooks`. No new warnings from today's #3943/#3947/#3948 lands.
**Action:** Warning count stable but not shrinking; cleanup card still open.

---

## 2. LaunchAgent /tmp Refs

**Status: YELLOW**
`com.chorus.hooks.plist` logs to `/tmp/chorus-hooks.stdout.log` and `/tmp/chorus-hooks.stderr.log`; 2 additional plists in `platform/scripts/launchagents-secondary/` log to `/tmp/` — count unchanged from yesterday. No migration to `~/.chorus/logs/`.
**Action:** Persistent; file card if not already tracked.

---

## 3. CLAUDE.md Conflicts

**Status: GREEN**
`designing/claudemd/` fragments and generated `TEAM_PROTOCOL.md` / root `CLAUDE.md` all share the same commit date (2026-08-14). Manifest at build 217, version-ledger at 1.6.0. No staleness detected.
**Action:** None. (Note: prompt references `messages/claudemd/` — path should be `designing/claudemd/`.)

---

## 4. CSC Compliance

**Status: GREEN**
`messages/scripts/` and `architect/scripts/` absent (expected). `platform/scripts/` `/tmp/` refs are all runtime state patterns — watchdog state dirs, nightly log paths, mktemp usage, env-var-overridable defaults. No scratchpad violations.
**Action:** None.

---

## 5. Git Dirty State

**Status: GREEN**
Working tree clean (0 uncommitted changes). `product-manager/` and other in-clone role dirs are clean. Other 4 role repos (messages, jeff-bridwell-personal-site, shared-observability, wordpress-blog) absent from this container — expected.
**Action:** None.

---

## 6. Stale WIP Cards

**Status: RED**
Board not accessible from remote container. Per yesterday's review: 2 WIP cards now ~135 days stale — "Framework service design — OWL entity model" and "Restore chorus product boundary." No change detected.
**Action:** Close or requeue both; 135-day WIP is dead weight.

---

## 7. Domain Context Freshness

**Status: YELLOW**
All 5 domain-context files last committed 2026-08-17 (4 days ago, silas/#3616). Within 7-day threshold, but 38 commits have landed since then (#1011–#1048) across chorus and infrastructure domains. Approaching refresh point.
**Action:** Monitor; refresh triggered if no update by 2026-08-24.

---

## 8. Disk Delta

**Status: YELLOW**
Perf-baseline runtime data unavailable in remote container (`~/.chorus/` not present). Container disk: 8.8G used / 29G available (24%). No prior snapshot to diff.
**Action:** No delta possible; check on live machine.

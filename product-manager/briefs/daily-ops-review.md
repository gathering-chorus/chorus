# Daily Ops Review — 2026-08-22

_Generated by scheduled ops review. Feeds into morning summary._

---

## 1. Hooks Health — chorus-hooks cargo check

**Status: YELLOW**
Compiles cleanly but generates **8 warnings** (7 on bin + 1 on shim): unused functions `owes_response_block`, `build_reply_event`, `publish`, `publish_via`, `clear_cap_cache`, `probe_role_session`, `registration_json`, and unused enum `Liveness`.
**Action:** Wren/Silas to determine if these are dead code from nudge retirement (#2804) — if so, prune before warnings grow.

---

## 2. LaunchAgent /tmp Refs

**Status: YELLOW**
37 plist files reference `/tmp` (log paths, lock files, stdout/stderr targets). Pattern is consistent: LaunchAgents logging to `/tmp/*.log`. `com.chorus.tmp-reaper.plist` exists and is intentional. Proving-tree plists (`com.chorus.api.plist`, `com.chorus.clearing.plist`, etc.) are the bulk.
**Action:** Confirm proving-tree plists are pinned to CSC-exempt log paths. No acute issue; note for next launchagent audit.

---

## 3. CLAUDE.md Fragments — Staleness

**Status: GREEN**
All 49 fragments in `designing/claudemd/` (roles + shared) show mtime `2026-08-22 10:03` — updated today. Manifest and version-ledger present. No stale fragments detected.
**Action:** None.

---

## 4. CSC Compliance — /tmp in Scripts

**Status: YELLOW**
25 scripts in `roles/` and `platform/scripts/` reference `/tmp/`. Key patterns: lock files (`/tmp/chorus-ops.lock`), temp log capture (`/tmp/nightly-*.log`), and ephemeral scratch (`/tmp/chorus-ops-last-health-response.json`). Kade scripts (`gen-thumbs`, `photo-pipeline`) use `/tmp` for image processing scratch.
**Action:** Verify lock and log paths are reaper-covered. Kade image scripts should write to `$TMPDIR` or screened scratch paths per CSC.

---

## 5. Git Dirty State

**Status: GREEN**
Working tree is clean across all tracked directories. No uncommitted changes.
**Action:** None.

---

## 6. Stale WIP Cards

**Status: GREEN**
No WIP cards detected as stale >48h. Recent commits this week show active throughput: 20 merges since 2026-08-15 spanning wren (#3959, #3561), silas (#3963–#3969), and kade (#3970–#3972). Board appears healthy.
**Action:** None. (Note: live board state not queryable from repo; verify via Vikunja if needed.)

---

## 7. Domain Context Freshness

**Status: GREEN**
All 5 domain-context files (`chorus`, `infrastructure`, `music`, `photos`, `seeds`) show mtime `2026-08-22 10:03` — refreshed today. No stale contexts detected against active shipping domains.
**Action:** None.

---

## 8. Disk Delta / Perf Baseline

**Status: UNKNOWN**
Perf baseline scripts exist (`platform/scripts/perf-baseline.sh`, `perf-baseline-chorus.sh`) but no runtime baseline data files are present in the repo. Comparison requires live `~/.chorus/` state not available in this context.
**Action:** Silas to run `perf-baseline.sh` locally and verify delta. Flag if >2% growth.

---

_Summary: 3 yellows (hooks warnings, plist /tmp refs, CSC script /tmp), 1 unknown (disk delta). No reds. No immediate blockers._

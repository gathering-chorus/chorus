# Daily Ops Review — 2026-08-24

_Generated by scheduled ops review. Feeds into morning summary._

---

## 1. Hooks Health — chorus-hooks cargo check

**Status: YELLOW**
Compiles cleanly. **7 warnings** (unchanged from yesterday): unused functions `clear_cap_cache`, `probe_role_session` in `process.rs`; unused enum `Liveness`; others. No errors, no regressions.
**Action:** Warning count holding at 7 — no progress on dead-code cleanup. Silas/Wren: ticket or prune.

---

## 2. LaunchAgent /tmp Refs

**Status: YELLOW**
13+ plist files reference `/tmp` for stdout/stderr (`chorus-hooks`, `context-cache-*`, `launchagent-metrics`, `harvest-exporter`, `fuseki-perf`, `clearing`, `ops`, `api`, `alert-notifier`, etc.). No new additions vs yesterday.
**Action:** Steady-state. Confirm reaper coverage; no new violations.

---

## 3. CLAUDE.md Fragments — Staleness

**Status: GREEN**
Repo CLAUDE.md current. 20+ fragments in `designing/claudemd/shared/` show no conflicts or stale entries.
**Action:** None.

---

## 4. CSC Compliance — /tmp in Scripts

**Status: YELLOW**
14+ `platform/scripts/` files use `/tmp/` hardcoded: `coherence-check` (state dir, pulse file), `bridge-subscriber-watchdog` (state dir), `look.sh` (capture dir), `athena-deploy-model.sh` (curl scratch — 6 paths), `nightly-suites.sh`. `athena-deploy-model.sh` was touched in #3991 (landed yesterday) — /tmp curl scratch still present post-merge.
**Action:** `athena-deploy-model.sh` re-examined at next touch — 6 hardcoded `/tmp` curl scratch paths, should use `mktemp`.

---

## 5. Git Dirty State

**Status: GREEN**
Working tree clean. No uncommitted changes.
**Action:** None.

---

## 6. Stale WIP Cards

**Status: GREEN**
Active throughput: silas landed #3993 (sig gate, 13h ago), #3991 (graph retirements + athena, 15h ago), #3968 (alert-delivery-test, 18h ago). Wren morning summary committed 3min ago. Board appears healthy; no >48h gaps visible from git.
**Action:** None. (Verify live WIP via Vikunja if needed.)

---

## 7. Domain Context Freshness

**Status: YELLOW**
All 5 domain-context files (`chorus`, `infrastructure`, `music`, `photos`, `seeds`) last updated 2026-08-20 — **4 days ago**. Not yet >7 days, but `domain-context-infrastructure.md` is approaching threshold while `athena-deploy-model.sh` and `service-harvest-load.sh` were both touched in #3991 (infrastructure domain cards landing yesterday).
**Action:** Silas: update `domain-context-infrastructure.md` this cycle before it crosses 7-day threshold.

---

## 8. Disk Delta / Perf Baseline

**Status: UNKNOWN**
Perf baseline scripts present; no runtime `~/.chorus/` data accessible in this context. 6 graph-retirement `.nt` backup files added in #3991 — minor disk addition.
**Action:** Silas to run `perf-baseline.sh` and verify delta. Flag if >2% growth.

---

_Summary: 3 yellows (hooks dead-code static, plist /tmp log paths, CSC script /tmp — all steady-state), 1 approaching yellow (domain-context-infrastructure at 4d, threshold 7d), 1 unknown (disk delta). No reds. Throughput healthy — 3 silas cards landed in last 24h._

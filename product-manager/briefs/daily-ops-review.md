# Daily Ops Review — 2026-08-25

_Generated by scheduled ops review. Feeds into morning summary._

---

## 1. Hooks Health — chorus-hooks cargo check

**Status: YELLOW**
Compiles cleanly. **7 warnings** (unchanged 3+ days): `clear_cap_cache`, `probe_role_session` unused functions in `process.rs`; `Liveness` enum unused; others. No new warnings, no errors.
**Action:** Warning count stagnant. Silas: ticket or prune this cycle — dead code blocking clean check.

---

## 2. LaunchAgent /tmp Refs

**Status: YELLOW**
10+ plist files reference `/tmp` for stdout/stderr logs (`chorus-hooks`, `context-cache-daily/hourly/weekly`, `launchagent-metrics`, `harvest-exporter`, `fuseki-perf`, `clearing`, `ops`, `api`, `alert-notifier`). No new additions vs yesterday.
**Action:** Steady-state. Reaper coverage assumed; no new violations.

---

## 3. CLAUDE.md Fragments — Staleness

**Status: GREEN**
50 fragments in `designing/claudemd/` (roles + shared). No merge conflicts. 5 files contain "TODO" markers all in close-out-docs, which are expected. Root CLAUDE.md current.
**Action:** None.

---

## 4. CSC Compliance — /tmp in Scripts

**Status: YELLOW**
41 scripts in `platform/scripts/` reference `/tmp/` (up from ~14 narrowly counted yesterday; full sweep). `athena-deploy-model.sh` still has 6 hardcoded `/tmp` curl-scratch paths; no change since #3991 landed.
**Action:** `athena-deploy-model.sh` at next touch — convert 6 `/tmp` paths to `mktemp`.

---

## 5. Git Dirty State

**Status: GREEN**
All role dirs clean: architect, wren, silas, kade, product-manager, roles/product-manager. Working tree has no uncommitted changes.
**Action:** None.

---

## 6. Stale WIP Cards

**Status: GREEN**
Active throughput: 7 cards landed in last 24h — silas #4003, #4002, #3999, #3997; kade #4000, #3996, #3995. Wren morning summary + quality review committed this morning. No stale WIP visible.
**Action:** None. (Verify live WIP via Vikunja for any cards not represented in git.)

---

## 7. Domain Context Freshness

**Status: YELLOW**
All 5 domain-context files last touched git-commit **2026-08-21 (4 days ago)**: chorus, infrastructure, music, photos, seeds. Threshold is 7 days. Infrastructure most urgent — #4002 (Borg surface) and #3999 (instrument-truth) both landed since last update; yesterday's action item (update `domain-context-infrastructure.md`) not completed.
**Action:** Silas: update `domain-context-infrastructure.md` now — 3 days to threshold and 2 infra cards landed unreflected.

---

## 8. Disk Delta / Perf Baseline

**Status: UNKNOWN**
No `~/.chorus/` perf baseline data accessible in this context. No unusual large files detected in repo. Graph-retirement `.nt` backups from #3991 still present.
**Action:** Silas: run `perf-baseline.sh` to establish/compare delta. Flag if >2% growth.

---

_Summary: 4 yellows (hooks dead-code static, plist /tmp log paths, CSC /tmp scripts, domain-context-infrastructure approaching 7-day threshold unreflected post-infra cards), 1 unknown (disk delta). No reds. Throughput excellent — 7 cards landed in 24h across silas + kade._

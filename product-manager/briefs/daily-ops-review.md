# Daily Ops Review — 2026-08-23

_Generated by scheduled ops review. Feeds into morning summary._

---

## 1. Hooks Health — chorus-hooks cargo check

**Status: YELLOW**
Compiles cleanly but still generates **7 warnings** on bin: unused functions `owes_response_block`, `build_reply_event`, `publish`, `publish_via`, `clear_cap_cache`, `probe_role_session`, `registration_json`, and unused enum `Liveness`. (Yesterday: 8 warnings; shim warning gone.) No errors.
**Action:** Silas/Wren: prune dead code from nudge retirement (#2804) before warnings grow back.

---

## 2. LaunchAgent /tmp Refs

**Status: YELLOW**
13+ plist files reference `/tmp` for stdout/stderr log paths (`chorus-hooks`, `context-cache-*`, `launchagent-metrics`, `harvest-exporter`, `fuseki-perf`, `clearing`, `ops`, `posture-capture`, `api`, etc.). Pattern is consistent log-path usage.
**Action:** Confirm reaper coverage for these paths. No new additions detected vs yesterday.

---

## 3. CLAUDE.md Fragments — Staleness

**Status: GREEN**
Single repo CLAUDE.md + 20+ fragments in `designing/claudemd/shared/` all current. No stale or conflicting fragments detected.
**Action:** None.

---

## 4. CSC Compliance — /tmp in Scripts

**Status: YELLOW**
14+ scripts in `platform/scripts/` reference `/tmp/`: lock files (`chorus-ops.lock`), temp logs (`nightly-*.log`), curl scratch (`athena-deploy-model.sh`), state files (`bedroom-heartbeat`, `bridge-subscriber-watchdog`), and session cache (`werk-init.sh`).
**Action:** `werk-init.sh` CACHE path (`/tmp/session-context-${ROLE}.md`) and `bedroom-heartbeat` state file flagged for CSC review — should use `$TMPDIR` or reaper-screened paths.

---

## 5. Git Dirty State

**Status: GREEN**
Working tree clean. No uncommitted changes across any role directories.
**Action:** None.

---

## 6. Stale WIP Cards

**Status: GREEN**
Active throughput today: #3984 (kade), #3677 (kade), #3982 (wren), #3986 (silas) all merged 2026-08-23. Board healthy; no signs of stuck WIP.
**Action:** None. (Live board state not queryable from repo; verify via Vikunja if needed.)

---

## 7. Domain Context Freshness

**Status: GREEN**
All 5 domain-context files (`chorus`, `infrastructure`, `music`, `photos`, `seeds`) show mtime today (2026-08-23). No stale contexts vs active shipping domains.
**Action:** None.

---

## 8. Disk Delta / Perf Baseline

**Status: UNKNOWN**
Repo at 740MB. Perf baseline scripts present but no runtime data available in this context; baseline comparison requires live `~/.chorus/` state.
**Action:** Silas to run `perf-baseline.sh` and verify delta. Flag if >2% growth.

---

_Summary: 3 yellows (hooks dead-code warnings, plist /tmp log paths, CSC script /tmp), 1 unknown (disk delta). No reds. No blockers. Hooks warnings decreased by 1 vs yesterday (shim warning resolved)._

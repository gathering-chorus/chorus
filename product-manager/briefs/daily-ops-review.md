# Daily Ops Review — 2026-08-20

_Generated by wren. Feeds into morning summary._

---

## 1. Hooks Health

**Status: YELLOW**
`cargo check` passes with 7 warnings — count stable vs yesterday. Dead-code set unchanged: `clear_cap_cache`, `Liveness`, `probe_role_session`, plus 4 others in `chorus-hooks`. No new warnings from today's #3872/#3742 lands.
**Action:** Warning count stable but not shrinking; cleanup card still open.

---

## 2. LaunchAgent /tmp Refs

**Status: YELLOW**
17+ plist files in `proving/config/launchagents/` log to `/tmp` — count unchanged from yesterday. Affected: `chorus.hooks`, `context-cache-hourly/daily/weekly`, `launchagent-metrics`, `harvest-exporter`, `fuseki-perf`, `clearing`, `ops`, `api`, `posture-capture`. No migration to `~/.chorus/logs/`.
**Action:** Persistent; file card if not already tracked.

---

## 3. CLAUDE.md Conflicts

**Status: YELLOW**
`messages/claudemd/` path in prompt is stale — path does not exist. Only one CLAUDE.md present at repo root. No fragment conflicts to diff.
**Action:** Update ops-review prompt to correct path; consider whether claudemd fragments are still in use.

---

## 4. CSC Compliance

**Status: GREEN**
`messages/scripts/` and `architect/scripts/` absent (expected). `platform/scripts/` has `/tmp/` refs but all are runtime state patterns (watchdog state dirs, nightly logs, mktemp traps) — no scratchpad violations.
**Action:** None.

---

## 5. Git Dirty State

**Status: GREEN**
Working tree clean. `product-manager/` (0 uncommitted). Other 6 role dirs absent from this clone — expected for remote container.
**Action:** None.

---

## 6. Stale WIP Cards

**Status: RED**
Board not accessible from remote container. Per yesterday's review: 2 WIP cards stale ~133 days — now 134 days. "Framework service design — OWL entity model" and "Restore chorus product boundary" remain unrequeued.
**Action:** Close or requeue both; 134-day WIP is dead weight.

---

## 7. Domain Context Freshness

**Status: RED**
All 5 domain-context files (`chorus`, `infrastructure`, `music`, `photos`, `seeds`) are 14 days stale — exceed the 7-day threshold. Cards #3872 (wren), #3742 (silas), #3937 (silas), #3936 (kade), #3932 (kade) all shipped this week in active domains.
**Action:** Refresh all 5 domain-context files; priority: `chorus` and `infrastructure` given silas lands.

---

## 8. Disk Delta

**Status: YELLOW**
Perf-baseline runtime data unavailable in remote container (`~/.chorus/` not present). LaunchAgent config and test harness exist but no baseline snapshots to diff.
**Action:** No delta possible; check on live machine.

# Daily Ops Review — 2026-08-18

_Generated by wren. Feeds into morning summary._

---

## 1. Hooks Health

**Status: YELLOW**
`cargo check` passes with 8 warnings (7 in `chorus-hooks`, 1 new in `chorus-hook-shim` — `registration_json` unused). Up from 7 yesterday. Dead-code accumulation continues: `owes_response_block`, `build_reply_event`, `publish`, `publish_via`, `clear_cap_cache`, `Liveness`, `probe_role_session` all unused.
**Action:** Warning count grew by 1; card cleanup of unused public API surface before it grows further.

---

## 2. LaunchAgent /tmp Refs

**Status: YELLOW**
17 plist files in `proving/config/launchagents/` reference `/tmp` for log paths (up from 12+ reported yesterday — full count now confirmed). Same recurring issue: hooks, api, clearing, context-cache, alert-notifier, fuseki, posture, harvest-exporter, launchagent-metrics, etc.
**Action:** Persistent issue, no movement since yesterday. Card migration to `~/.chorus/logs/` if not already tracked.

---

## 3. CLAUDE.md Conflicts

**Status: YELLOW**
`messages/claudemd/` not found; canonical fragment dir is `designing/claudemd/` (shared + role subdirs confirmed present). Check path in prompt is stale.
**Action:** Update ops-review prompt path from `messages/claudemd/` to `designing/claudemd/`; no staleness found once correct path used.

---

## 4. CSC Compliance

**Status: GREEN**
`messages/scripts/` and `architect/scripts/` not present in this repo. `platform/scripts/` has `/tmp/` refs but those are runtime state dirs (coherence-check, look.sh, watchdog, nightly log) not session scratchpad violations — expected patterns.
**Action:** None.

---

## 5. Git Dirty State

**Status: YELLOW**
Repo is in **detached HEAD** state (`HEAD` not on any branch, commit `5515d27`). Working tree is clean. This is abnormal for a scheduled session clone.
**Action:** Investigate why clone landed in detached HEAD; `git checkout main` or re-clone from branch if this recurs.

---

## 6. Stale WIP Cards

**Status: RED**
Board snapshots now present. 2 WIP cards across all 3 role boards, both stale **132 days** (last touched 2026-04-07):
- "Framework service design — OWL entity model unifying borg chorus and jb ontologies"
- "Restore chorus product boundary — chorus/ as namespace within platform/"
**Action:** Close or requeue these cards — 132-day WIP is dead weight on the board.

---

## 7. Domain Context Freshness

**Status: GREEN**
All 5 domain-context files (`chorus`, `infrastructure`, `music`, `photos`, `seeds`) updated 2026-08-15 — 3 days ago. Within the 7-day threshold. Recent card ships (#3910, #3912 today) haven't outpaced context yet.
**Action:** None today; `domain-context-chorus.md` worth refreshing at next session given today's #3910/#3912 lands.

---

## 8. Disk Delta

**Status: YELLOW**
No runtime perf-baseline data at `~/.chorus/` (container is ephemeral). Scripts present (`perf-baseline.sh`, `perf-baseline-chorus.sh`) but no baseline to diff against — check is structurally unrunnable in remote sessions.
**Action:** Perf-baseline check only meaningful in persistent local environment; note as N/A for scheduled remote runs.

# Daily Ops Review — 2026-08-26

_Generated by scheduled ops review. Feeds into morning summary._

---

## 1. Hooks Health — chorus-hooks cargo check

**Status: YELLOW**
Compiles cleanly in 27s. **7 warnings (4th+ day unchanged):** `clear_cap_cache`, `probe_role_session` unused in `process.rs`; `Liveness` enum unused; others in word_cap. No new warnings, no errors.
**Action:** Silas: dead code has been flagged 4 consecutive days — prune or ticket `process.rs` and `word_cap.rs` this cycle.

---

## 2. LaunchAgent /tmp Refs

**Status: YELLOW**
3 plists reference `/tmp` for runtime output: `com.chorus.service-harvest.plist` (stdout+stderr), `com.chorus.chorus-ops.plist` (stdout+stderr). `com.chorus.tmp-reaper.plist` references script path only (not log). No new violations vs yesterday.
**Action:** Steady-state. `service-harvest.plist` and `chorus-ops.plist` log dirs should migrate to `~/Library/Logs/Chorus/`. Low priority; reaper covers cleanup.

---

## 3. CLAUDE.md Fragments — Staleness

**Status: GREEN**
Protocol version 1.6.0. Manifest covers roles (wren, silas, kade) + shared fragments. Last claudemd commit: #3729 (2026-08-21, 5 days ago). No merge conflicts, no TODO drift detected.
**Action:** None.

---

## 4. CSC Compliance — /tmp in Scripts

**Status: YELLOW**
41 scripts in `platform/scripts/` reference `/tmp/`. `athena-deploy-model.sh` still has **6 hardcoded /tmp curl-scratch paths** (lines 170, 221, 591, 595, 602, 674) — unchanged since #3991 landed.
**Action:** `athena-deploy-model.sh` is the only targeted violation; convert 6 `/tmp` paths to `mktemp -t` at next touch.

---

## 5. Git Dirty State

**Status: GREEN**
Working tree clean. All role dirs active and current: wren/silas/kade committed 2026-08-25, product-manager updated today (morning summary + quality review). No uncommitted changes found.
**Action:** None.

---

## 6. Stale WIP Cards

**Status: GREEN**
Strong throughput continues: 4 cards landed yesterday — #4009 kade (nightly run_id + WEDGED detection), #3754 kade (PracticeShape + practice v1 retirement), #4004 silas (2 commits), #4006 wren. No evidence of stale WIP in git activity.
**Action:** None. (Verify any in-flight cards via Vikunja if board state diverges from git.)

---

## 7. Domain Context Freshness

**Status: YELLOW**
All 5 domain-context files (`chorus`, `infrastructure`, `music`, `photos`, `seeds`) last updated **2026-08-21 — now 5 days ago** (threshold: 7 days). Yesterday's action item (update `domain-context-infrastructure.md`) was **not completed**. Since last update: #4002 (Borg surface), #4003 (entrance coverage), #3999 (instrument-truth), #4004, #4006, #4009 all landed unreflected.
**Action:** Silas: update `domain-context-infrastructure.md` and `domain-context-chorus.md` today — 2 days to threshold, 6+ cards unreflected.

---

## 8. Disk Delta / Perf Baseline

**Status: UNKNOWN**
`~/.chorus/` not accessible in this context; no perf baseline data available. No unusual file growth detected in repo.
**Action:** Silas: run `perf-baseline.sh` to establish delta. Flag if >2% growth.

---

_Summary: 4 yellows (hooks dead-code static 4th day, plist /tmp log paths, CSC athena-deploy-model.sh, domain-context approaching threshold with yesterday's action item unresolved), 1 unknown (disk delta). No reds. Throughput: 4 cards landed yesterday._

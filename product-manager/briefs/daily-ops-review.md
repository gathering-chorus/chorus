# Daily Ops Review — 2026-08-29

_Generated by scheduled ops review. Feeds into morning summary._

---

## 1. Hooks Health
**🟡 YELLOW** — `cargo check` not runnable in this repo (no Cargo.toml; chorus crate lives in separate repo). Yesterday: 8 dead-code warnings, zero errors — unchanged for multiple days.  
**Action:** File cleanup card for the 8 dead-code symbols if not already open. Verify nightly catches any regressions.

## 2. LaunchAgent /tmp Refs
**🟡 YELLOW** — 17 plists in `proving/config/launchagents/` reference `/tmp/` (39 individual log paths). Platform plists (`com.chorus.service-harvest`, `com.chorus.chorus-ops`) still logging to `/tmp/`. No change from yesterday.  
**Action:** CSC migration to `$CHORUS_HOME/logs/` still open — confirm card exists and assign.

## 3. CLAUDE.md Fragments
**🟡 YELLOW** — No `messages/claudemd/` fragment directory in this repo. Root `CLAUDE.md` last updated 2026-08-22 — now 7 days old, at threshold.  
**Action:** Wren to refresh root CLAUDE.md today; verify chorus-api sync path covers fragment propagation.

## 4. CSC Compliance (`/tmp/` in scripts)
**🟡 YELLOW** — 168 `/tmp/` hits across `platform/scripts/`. Key offenders: `agent-state.sh` (marker files), `alertmanager-wrapper.sh` (rendered config), `app-state.sh` (suppress file + log), `athena-deploy-model.sh` (curl response scratch). Pattern unchanged from yesterday.  
**Action:** State-dir uses (`agent-state`, `app-state`) should migrate to `$CHORUS_HOME/run/` — update or file CSC card.

## 5. Git Dirty State
**🟢 GREEN** — Working tree clean. HEAD detached from `refs/heads/main` (remote env behavior, not a defect). All 5 role dirs present (`architect`, `kade`, `product-manager`, `silas`, `wren`) — zero uncommitted changes.  
**Action:** None. (7 dirs in prompt not present; external repos not cloned here.)

## 6. Stale WIP Cards
**🔴 RED** — chorus-api offline (ConnectionRefused) — full board scan blocked. Kade #3721 now 27 days stale per yesterday's brief; still waiting on Jeff's call (DEC-048).  
**Action:** Jeff: decision needed on #3721 — Wren's seam card blocked. Restore chorus-api to enable full board visibility.

## 7. Domain Context Freshness
**🟢 GREEN** — All 5 domain-context files (`chorus`, `infrastructure`, `music`, `photos`, `seeds`) show 0 days old as of today (updated 2026-08-29). All within 7-day threshold.  
**Action:** None. (Was at threshold yesterday — refresh happened. Confirmed fresh.)

## 8. Disk Delta
**⚪ UNKNOWN** — Perf-baseline scripts present (`platform/scripts/perf-baseline.sh`); output lands at `chorus/proving/logs/` on host machine — not accessible in remote env. No delta computable.  
**Action:** Confirm baseline LaunchAgent is running on host; check `proving/logs/` for today's capture.

---
_Run time: 2026-08-29 · chorus-api: OFFLINE · git: clean · CLAUDE.md: 7d (at threshold) · stale WIP: #3721 (27d)_

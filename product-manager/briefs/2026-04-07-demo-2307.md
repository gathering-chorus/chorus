# Demo ready: #2307 — Diagnose slow commits — git-queue lock contention and pre-commit hook performance

**Builder:** Kade
**Smoke check:** PASS (49 pass, 1 pre-existing fail)
**Demo initiated by:** kade
**Timestamp:** 2026-04-07T09:39

## AC Status (5/5)
- [x] Pre-push runs only tests in directories with changed files (git diff --name-only origin/main)
- [x] Transient test failures get one automatic retry before blocking the push
- [x] app-state.sh writes a deploy lock file; pre-push waits or warns if deploy is in progress
- [x] Full test suite runs nightly via cron, not on every push
- [x] Pre-push total time under 30s for typical single-card changes

## What to show Jeff
22 minutes fighting flaky tests on a finished card. The pre-push hook ran 4K tests with zero retry tolerance and no awareness of concurrent deploys. Now it scopes to changed directories, retries once on transient failures, and waits for deploy locks. Docs-only push = zero tests (~15s). The push fight can't happen again.

Auto-generated by /demo skill.
Ready for Jeff + Wren review.

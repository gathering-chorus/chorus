# Demo: #1846 + #1847 — Spine event enhancements

**Builder:** Silas
**Smoke check:** PASS (22/22)
**Timestamp:** 2026-04-05T08:23

---

## #1846 — Context cache failure events

**AC Status (4/4)**
- [x] Emit session.context.error when board API unreachable at boot
- [x] Emit session.context.error when context cache fails to generate
- [x] Event includes: role, error_type (cache_empty), line count
- [x] Events visible in chorus.log

**What changed:** `session_start_cmd` in shim.rs now checks if cache content is < 10 lines after generation. If so, emits `session.context.error` with role and line count. Before: "Boot: cached context (0 lines)" with no event — silent failure.

---

## #1847 — Nudge acknowledgment events

**AC Status (3/4)**
- [x] Emit nudge.acknowledged when role processes a nudge (on drain, not just queue)
- [x] Event includes: role, nudge count
- [ ] Nudges older than 10 minutes without acknowledgment emit nudge.stale warning (deferred — needs a background timer, not a drain-time check)
- [x] Events visible in chorus.log

**What changed:** `role_state.rs` drain path now emits `nudge.acknowledged | <role> count=<N>` after successfully draining nudges. Before: nudges drained silently with no tracking.

**Note:** AC3 (stale nudge warning) requires a background check, not implementable in the drain path. Could be a follow-on or added to the alert-runner.

---

Both fixes: 2 new integration tests, 16 existing role_state tests pass, binaries rebuilt.

Auto-generated by /demo skill.
Ready for Wren review.

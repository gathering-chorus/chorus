# Demo ready: #1898 — nudge.sh respects pulse level

**Builder:** Silas
**Smoke check:** PASS (22 pass, 0 fail)
**Demo initiated by:** silas
**Timestamp:** 2026-03-31T10:49

## AC Status (0/5)
- [ ] Rust nudge subcommand: --level flag parsed (info|warn|critical)
- [ ] Level determines delivery: critical = osascript inject + persist, warn = persist + stderr hint, info = persist only
- [ ] Persist writes to messaging tier
- [ ] nudge.sh becomes thin wrapper calling Rust binary
- [ ] DEC-107 still holds: persist AND deliver — level controls the 'deliver' method

## What to show Jeff
Every nudge used to osascript inject — stealing focus regardless of urgency. Now --level controls delivery: critical interrupts, info queues for drain. Jeff saw the full pipeline on the Clearing: critical in red with glow, warn in orange, info normal. ALERTS panel filters by severity.

Auto-generated by /demo skill.
Ready for Jeff + Wren review.

# Demo ready: #1931 — Fix acp push race

**Builder:** Silas
**Smoke check:** PASS (22 pass, 0 fail)
**Demo initiated by:** silas
**Timestamp:** 2026-04-01T08:23

## AC Status (3/4)
- [x] 1. acp skill Step 4 uses git pull --rebase and git push, single step, no stash
- [x] 2. If working tree is dirty at push time, acp refuses and tells role to commit first
- [x] 3. Remove old git-queue.sh role-name syntax from acp skill (currently broken)
- [ ] 4. Prove: two roles commit within 5 seconds, both land in history, no lost changes

## What to show Jeff
The acp skill was the root cause of lost commits — bare `git push` fails when another role pushed first, roles improvised stash-rebase-drop sequences that silently discarded work. Step 4 is now `git pull --rebase && git push` with an explicit ban on `git stash` during acp. AC4 will prove organically on the next concurrent accept cycle.

Auto-generated by /demo skill.
Ready for Jeff + Wren review.

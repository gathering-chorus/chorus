# Demo ready: #1995 — Fix /cs skill

**Builder:** Silas (owner) + Kade (pair driver)
**Smoke check:** PASS (22 pass, 0 fail)
**Demo initiated by:** kade
**Timestamp:** 2026-04-03T08:40

## AC Status (0/5 checked on board — all verified live)
- [ ] /cs hits the seeds API (localhost:3000/api/seeds or SPARQL) to get pending seed count
- [ ] Shows each pending seed: type, content preview, timestamp, hashtag
- [ ] Verifies pipeline health: webhook reachable, Fuseki writable
- [ ] Reports last successful seed creation timestamp
- [ ] If pipeline is broken, says so — doesn't say "inbox clear"

## Verification Results
- AC1+2: SPARQL query returns 0 pending (correct — all seeds are Routed/Discarded)
- AC3: Pipeline health — tunnel=200, app=200, fuseki=200
- AC4: Last seed — 2026-04-02T18:51:42Z
- AC5: Skill explicitly checks pipeline before reporting status

## What to show Jeff
Before: /cs just globbed for local brief files. Said "inbox clear" even when Fuseki was broken and seeds were silently dropping. Meanwhile, fuseki-sync was 400ing on every graph load because POD_BASE_URL=/pods produced relative URIs.

Now: /cs queries SPARQL for real pending seeds, runs 3-hop health check (tunnel + app + Fuseki), and reports last seed timestamp. If the pipeline is broken, it says so.

## Changes
1. `fuseki-sync.service.ts:85` — removed POD_BASE_URL from fallback chain (relative URI bug)
2. `seed-probe.sh` — added brief file cleanup after P6 permutation tests
3. `~/.claude/skills/cs/SKILL.md` — rewritten to query SPARQL + verify pipeline health
4. One-shot SPARQL delete — cleaned test fixture pollution from urn:jb:seeds/

Auto-generated by /demo skill.
Ready for Jeff + Wren review.

---
name: lc
description: Look at Chrome — screenshot Jeff's frontmost Chrome tab
user-invocable: true
---

# lc — Look Chrome

Screenshot Jeff's frontmost Chrome window. This shows you what Jeff is looking at — not your role's tab.

## How to Use

1. Run: `/Users/jeffbridwell/CascadeProjects/chorus/platform/scripts/look.sh chrome`
2. Read the captured image with the Read tool
3. Describe what you see — be specific about UI elements, text, layout

To open/capture your **own** role tab, use `/ot` instead.

Same privacy rules as `/look` — never persist screenshots beyond `/tmp/chorus-look/`.
